# Hearth & Honey Website Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship a four-page static bakery site for Hearth & Honey to GitHub Pages at hearthhoneybakery.com that converts visitors into email or Instagram orders, meets WCAG 2.2 AA, and is fully machine-readable (JSON-LD, sitemap, llms.txt).

**Architecture:** Hand-written HTML, one CSS file, two ES-module scripts, served from the root of `main` by GitHub Pages with no build step. A `tools/` folder holds Node scripts that process the supplied images, fetch self-hosted fonts, and run verification (unit tests, HTML validation, axe, Lighthouse, link and structured-data checks). Tools are never needed to deploy.

**Tech Stack:** HTML5, CSS (custom properties, `clamp()`), ES modules, Node 24, sharp, potrace, png-to-ico, html-validate, playwright-core (system Chrome), @axe-core/playwright, lighthouse.

**Spec:** `docs/superpowers/specs/2026-09-06-hearth-and-honey-website-design.md`

## Global Constraints

- Brand colors exactly `#FAF3E8` (cream) and `#C84C5A` (red). Derived tokens: `#F3E6D3`, `#FFFCF7`, `#A93A48`, `#8E2C39`, `#3B2A22`, `#6B5A50`, `#E3A93D` (decorative only).
- No prices anywhere. No phone number. No street address; city only ("Draper, Utah", postal 84020 in schema only).
- Every order path ends at `mailto:orders@hearthhoneybakery.com` or Instagram `https://www.instagram.com/hearth.honey.bakery/` / DM `https://ig.me/m/hearth.honey.bakery`.
- Food Freedom Act text, verbatim: "Made in a home kitchen that is not licensed or inspected by the Utah Department of Agriculture and Food or a local health department."
- Canonical origin `https://hearthhoneybakery.com`. Clean URLs `/`, `/menu/`, `/about/`, `/order/`. All internal links and asset paths are root-absolute (`/assets/...`).
- WCAG 2.2 AA. Text contrast ≥ 4.5:1, UI/large text ≥ 3:1, tap targets ≥ 44px, reduced-motion respected.
- Lighthouse mobile targets: Performance ≥ 95, Accessibility 100, Best Practices ≥ 95, SEO 100.
- Use `&amp;` for every ampersand in HTML text and attributes.
- Before Task 7, invoke `frontend-design:frontend-design`. Before claiming completion, invoke `superpowers:verification-before-completion`.
- Commit after every task with message prefix `feat:`, `chore:`, `docs:`, or `test:`. Commits end with the Co-Authored-By and Claude-Session trailers used in this session.
- Lead time is 2 days. It lives in `assets/js/order-mail.js` (`LEAD_DAYS`) and in the FAQ copy on `/order/`; change both together.

## File Map

| Path | Responsibility |
|---|---|
| `index.html` | Home page |
| `menu/index.html` | Menu page |
| `about/index.html` | About Megan |
| `order/index.html` | Order form + FAQ |
| `404.html` | Not found |
| `assets/css/site.css` | All styles |
| `assets/js/site.js` | Mobile nav disclosure |
| `assets/js/order-mail.js` | Pure order helpers (tested in Node) |
| `assets/js/order.js` | Order form DOM glue |
| `assets/img/*` | Logo, illustrations, photos, social image, icons (generated by tools) |
| `assets/fonts/*.woff2` | Self-hosted Fraunces + Figtree |
| `robots.txt`, `sitemap.xml`, `llms.txt`, `site.webmanifest`, `CNAME`, `.nojekyll`, `favicon.ico` | Crawler and platform files |
| `tools/package.json` | Dev dependencies and scripts |
| `tools/source/` | Original supplied images |
| `tools/make-illustrations.mjs` | Writes the SVG roll, swirl, favicon |
| `tools/build-images.mjs` | Logo cleanup + trace, photo sizes, OG image, icons |
| `tools/fetch-fonts.mjs` | Downloads woff2 files |
| `tools/serve.mjs` | Static server with clean URLs (used by verifiers) |
| `tools/tests/order-mail.test.mjs` | Unit tests |
| `tools/verify/{contrast,links,jsonld,axe,lighthouse}.mjs` | Verification scripts |
| `tools/.htmlvalidate.json` | html-validate config |
| `README.md` | How to edit, add products, deploy, DNS |

Code blocks below carry a `path=` attribute naming the file they belong to. Write each block to that path verbatim.

---

### Task 1: Tooling scaffold and source assets

**Files:**
- Create: `.gitignore`, `tools/package.json`, `tools/source/logo.png`, `tools/source/megan.jpg`, `tools/source/label.jpg`

**Interfaces:**
- Produces: `tools/node_modules` with sharp, potrace, png-to-ico, html-validate, playwright-core, @axe-core/playwright, lighthouse, chrome-launcher. npm scripts named in `package.json` below are used by every later task.

- [ ] **Step 1: Write `.gitignore`**

```text path=.gitignore
tools/node_modules/
tools/reports/
.DS_Store
Thumbs.db
```

- [ ] **Step 2: Write `tools/package.json`** (versions are filled in by the install in Step 4)

```json path=tools/package.json
{
  "name": "hearth-and-honey-tools",
  "private": true,
  "type": "module",
  "description": "Local helper scripts for the Hearth & Honey site. Not needed to deploy.",
  "scripts": {
    "build:illustrations": "node make-illustrations.mjs",
    "build:images": "node build-images.mjs",
    "fetch:fonts": "node fetch-fonts.mjs",
    "serve": "node serve.mjs",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "verify:contrast": "node verify/contrast.mjs",
    "verify:html": "html-validate --config .htmlvalidate.json \"../*.html\" \"../*/index.html\"",
    "verify:links": "node verify/links.mjs",
    "verify:jsonld": "node verify/jsonld.mjs",
    "verify:axe": "node verify/axe.mjs",
    "verify:lh": "node verify/lighthouse.mjs",
    "verify": "npm run test && npm run verify:contrast && npm run verify:html && npm run verify:links && npm run verify:jsonld && npm run verify:axe && npm run verify:lh"
  }
}
```

- [ ] **Step 3: Copy the supplied images into `tools/source/`**

```bash
mkdir -p tools/source
cp "/c/Users/Cade/.claude/uploads/b0ef04bc-bf97-4733-ab3c-23a3edce191c/19aabceb-image.png" tools/source/logo.png
cp "/c/Users/Cade/.claude/uploads/b0ef04bc-bf97-4733-ab3c-23a3edce191c/331823ee-image.jpg" tools/source/label.jpg
cp "/c/Users/Cade/.claude/uploads/b0ef04bc-bf97-4733-ab3c-23a3edce191c/75851ffa-image.jpg" tools/source/megan.jpg
```

- [ ] **Step 4: Install dev dependencies**

Run: `cd tools && npm install --save-dev sharp potrace png-to-ico html-validate playwright-core @axe-core/playwright lighthouse chrome-launcher`
Expected: `package.json` gains a `devDependencies` block; `npm ls --depth=0` lists all eight.

- [ ] **Step 5: Commit**

```bash
git add .gitignore tools/package.json tools/package-lock.json tools/source
git commit -m "chore: tooling scaffold and source assets"
```

---

### Task 2: Palette contrast check

**Files:**
- Create: `tools/verify/contrast.mjs`

**Interfaces:**
- Produces: the verified token values used in `site.css` (Task 7). If any pair fails, darken that token until it passes and carry the new value into Task 7.

- [ ] **Step 1: Write the checker**

```js path=tools/verify/contrast.mjs
// WCAG 2.x contrast check for every text/background pair the site uses.
// Run: npm run verify:contrast   (exit 1 on any failure)
const TOKENS = {
  cream: '#FAF3E8', cream2: '#F3E6D3', paper: '#FFFCF7',
  red: '#C84C5A', redInk: '#A93A48', redDeep: '#8E2C39',
  ink: '#3B2A22', inkSoft: '#6B5A50', white: '#FFFFFF',
};
// [foreground, background, minimum ratio, where it is used]
const PAIRS = [
  ['ink', 'cream', 4.5, 'body text'],
  ['ink', 'cream2', 4.5, 'body text on bands'],
  ['ink', 'paper', 4.5, 'body text on cards'],
  ['inkSoft', 'cream', 4.5, 'secondary text'],
  ['inkSoft', 'cream2', 4.5, 'secondary text on bands'],
  ['inkSoft', 'paper', 4.5, 'secondary text on cards'],
  ['redInk', 'cream', 4.5, 'links, h3, eyebrows'],
  ['redInk', 'cream2', 4.5, 'links on bands'],
  ['redInk', 'paper', 4.5, 'links on cards, light button label'],
  ['red', 'cream', 3, 'h1/h2 (large text), focus ring, borders'],
  ['red', 'cream2', 3, 'large headings on bands'],
  ['red', 'paper', 3, 'large headings on cards'],
  ['white', 'red', 4.5, 'primary button label'],
  ['white', 'redDeep', 4.5, 'primary button hover label'],
  ['cream', 'redDeep', 4.5, 'Instagram band text'],
  ['redInk', 'white', 4.5, 'band button label'],
];

function luminance(hex) {
  const [r, g, b] = hex.match(/[0-9a-f]{2}/gi)
    .map((h) => parseInt(h, 16) / 255)
    .map((c) => (c <= 0.03928 ? c / 12.92 : ((c + 0.055) / 1.055) ** 2.4));
  return 0.2126 * r + 0.7152 * g + 0.0722 * b;
}
export function ratio(a, b) {
  const [hi, lo] = [luminance(a), luminance(b)].sort((x, y) => y - x);
  return (hi + 0.05) / (lo + 0.05);
}

let failed = 0;
for (const [fg, bg, min, note] of PAIRS) {
  const r = ratio(TOKENS[fg], TOKENS[bg]);
  const ok = r >= min;
  if (!ok) failed++;
  console.log(`${ok ? 'PASS' : 'FAIL'}  ${fg} on ${bg}  ${r.toFixed(2)}:1  (min ${min})  ${note}`);
}
if (failed) {
  console.error(`\n${failed} pair(s) below threshold`);
  process.exit(1);
}
console.log('\nAll contrast pairs pass');
```

- [ ] **Step 2: Run it**

Run: `cd tools && npm run verify:contrast`
Expected: every line `PASS`, final line `All contrast pairs pass`. Expected approximate values: red on cream 4.1, redInk on cream 5.6, white on red 4.53, cream on redDeep 7.4.

- [ ] **Step 3: Commit**

```bash
git add tools/verify/contrast.mjs
git commit -m "test: palette contrast checker"
```

---

### Task 3: Illustrations (SVG) generator

**Files:**
- Create: `tools/make-illustrations.mjs`
- Generates: `assets/img/cinnamon-roll.svg`, `assets/img/swirl.svg`, `assets/img/favicon.svg`

**Interfaces:**
- Produces: `cinnamon-roll.svg` with viewBox `0 0 320 250` (used by `<img width="320" height="250">`), `swirl.svg` 24×24 black stroke (used as a CSS mask with `currentColor`), `favicon.svg` 64×64.

- [ ] **Step 1: Write the generator**

```js path=tools/make-illustrations.mjs
// Writes the hand-drawn-style SVG assets. Re-run after editing: npm run build:illustrations
import { writeFileSync, mkdirSync } from 'node:fs';
import { fileURLToPath } from 'node:url';

const OUT = fileURLToPath(new URL('../assets/img/', import.meta.url));
mkdirSync(OUT, { recursive: true });

const RED = '#C84C5A';
const CREAM = '#FAF3E8';
const CREAM2 = '#F3E6D3';
const PAPER = '#FFFCF7';

/** Archimedean spiral squashed to an ellipse, as an SVG path string. */
function spiral({ cx, cy, rx, ry, turns, points = 240, start = 0.06, rotate = -0.6 }) {
  const T = turns * Math.PI * 2;
  const pts = [];
  for (let i = 0; i <= points; i++) {
    const t = (i / points) * T;
    const k = start + (1 - start) * (t / T);
    pts.push([cx + k * rx * Math.cos(t + rotate), cy + k * ry * Math.sin(t + rotate)]);
  }
  return 'M' + pts.map(([x, y]) => `${x.toFixed(1)} ${y.toFixed(1)}`).join(' L');
}

const roll = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 320 250" role="img" aria-labelledby="hh-roll-title">
  <title id="hh-roll-title">Illustrated cinnamon roll with glaze</title>
  <g stroke="${RED}" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
    <path d="M60 122 V174 A100 60 0 0 0 260 174 V122" fill="${CREAM2}"/>
    <path d="M62 150 C90 166 130 172 160 172 C190 172 230 166 258 150" fill="none" stroke-width="2.5" opacity="0.6"/>
    <ellipse cx="160" cy="122" rx="100" ry="60" fill="${CREAM2}"/>
    <path d="M78 118 C84 80 122 62 160 64 C206 66 244 88 246 120 C247 138 236 150 230 162 C224 174 212 174 212 160 C212 150 218 140 208 138 C192 136 182 152 176 168 C172 182 158 182 156 168 C154 152 150 146 140 144 C122 142 114 158 110 172 C106 184 94 182 94 168 C94 152 98 142 86 136 C76 130 74 124 78 118 Z" fill="${PAPER}"/>
    <path d="${spiral({ cx: 160, cy: 118, rx: 76, ry: 44, turns: 2.75 })}" fill="none"/>
  </g>
</svg>
`;

const swirl = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="${spiral({ cx: 12, cy: 12, rx: 10, ry: 10, turns: 2.25, points: 120, start: 0.05 })}" fill="none" stroke="#000" stroke-width="2.4" stroke-linecap="round"/></svg>
`;

const favicon = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64"><rect width="64" height="64" rx="14" fill="${CREAM}"/><path d="${spiral({ cx: 32, cy: 32, rx: 22, ry: 22, turns: 2.5, points: 160, start: 0.05 })}" fill="none" stroke="${RED}" stroke-width="5.5" stroke-linecap="round"/></svg>
`;

writeFileSync(OUT + 'cinnamon-roll.svg', roll);
writeFileSync(OUT + 'swirl.svg', swirl);
writeFileSync(OUT + 'favicon.svg', favicon);
console.log('wrote cinnamon-roll.svg, swirl.svg, favicon.svg');
```

- [ ] **Step 2: Generate and render for a visual check**

Run: `cd tools && npm run build:illustrations && node -e "import('sharp').then(async({default:s})=>{await s('../assets/img/cinnamon-roll.svg',{density:200}).flatten({background:'#FAF3E8'}).png().toFile('reports/roll-preview.png');await s('../assets/img/favicon.svg',{density:400}).resize(128,128).png().toFile('reports/favicon-preview.png');console.log('ok')})"`
Then view `tools/reports/roll-preview.png` and `tools/reports/favicon-preview.png` with the Read tool.
Expected: a roll with a visible spiral, glaze with three drips over the front, readable favicon spiral. Adjust the glaze path or spiral parameters if the drawing looks off, re-run, re-view.

- [ ] **Step 3: Commit**

```bash
git add tools/make-illustrations.mjs assets/img/cinnamon-roll.svg assets/img/swirl.svg assets/img/favicon.svg
git commit -m "feat: SVG illustrations (roll, swirl, favicon)"
```

---
### Task 4: Logo, photos, social image, icons

**Files:**
- Create: `tools/build-images.mjs`
- Generates: `assets/img/logo.svg`, `logo-cream.svg`, `logo.png`, `logo-600.png`, `megan-portrait-{480,800}.{webp,jpg}`, `megan-square-{300,600}.{webp,jpg}`, `cinnamon-roll-1200.png`, `og-image.png`, `favicon-32.png`, `apple-touch-icon.png`, `icon-192.png`, `icon-512.png`, and root `favicon.ico`

**Interfaces:**
- Consumes: `assets/img/cinnamon-roll.svg`, `assets/img/favicon.svg` from Task 3.
- Produces: `logo.svg` normalized to viewBox `0 0 2500 1000` (5:2), so pages use `<img width="160" height="64">` in the header and `width="200" height="80"` in the footer. `og-image.png` is 1200×630. Portraits are 3:4, squares 1:1.

- [ ] **Step 1: Write the build script**

```js path=tools/build-images.mjs
// Processes the supplied images into everything the site needs. Run: npm run build:images
import sharp from 'sharp';
import potrace from 'potrace';
import pngToIco from 'png-to-ico';
import { readFileSync, writeFileSync, mkdirSync } from 'node:fs';
import { fileURLToPath } from 'node:url';

const SRC = fileURLToPath(new URL('./source/', import.meta.url));
const OUT = fileURLToPath(new URL('../assets/img/', import.meta.url));
const ROOT = fileURLToPath(new URL('../', import.meta.url));
mkdirSync(OUT, { recursive: true });

const RED = { r: 200, g: 76, b: 90 };
const CANVAS = { width: 2500, height: 1000 }; // 5:2, matches <img width="160" height="64">

// 1. Logo: the PNG is red ink on white. Recover alpha from the green channel
//    (white = 255, ink = 76), paint every pixel brand red, trim, center on a 5:2 canvas, trace.
async function buildLogo() {
  const { data, info } = await sharp(SRC + 'logo.png').ensureAlpha().raw().toBuffer({ resolveWithObject: true });
  const rgba = Buffer.alloc(info.width * info.height * 4);
  for (let i = 0; i < data.length; i += 4) {
    const a = Math.max(0, Math.min(1, (255 - data[i + 1]) / (255 - RED.g)));
    rgba[i] = RED.r; rgba[i + 1] = RED.g; rgba[i + 2] = RED.b; rgba[i + 3] = Math.round(a * 255);
  }
  const trimmed = await sharp(rgba, { raw: { width: info.width, height: info.height, channels: 4 } })
    .trim({ threshold: 12 }).png().toBuffer();
  const meta = await sharp(trimmed).metadata();
  const scale = Math.min((CANVAS.width * 0.94) / meta.width, (CANVAS.height * 0.9) / meta.height);
  const w = Math.round(meta.width * scale);
  const h = Math.round(meta.height * scale);
  const placed = await sharp({ create: { ...CANVAS, channels: 4, background: { r: 0, g: 0, b: 0, alpha: 0 } } })
    .composite([{ input: await sharp(trimmed).resize(w, h).toBuffer(), left: Math.round((CANVAS.width - w) / 2), top: Math.round((CANVAS.height - h) / 2) }])
    .png().toBuffer();
  await sharp(placed).resize(1000, 400).png().toFile(OUT + 'logo.png');
  await sharp(placed).resize(600, 240).png().toFile(OUT + 'logo-600.png');

  const forTrace = await sharp(placed).flatten({ background: '#ffffff' }).png().toBuffer();
  const svg = await new Promise((resolve, reject) =>
    potrace.trace(forTrace, { threshold: 165, turdSize: 8, optTolerance: 0.3, alphaMax: 1, color: '#C84C5A', background: 'transparent' },
      (err, out) => (err ? reject(err) : resolve(out))));
  const normalized = svg.replace(/<svg[^>]*>/, `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 ${CANVAS.width} ${CANVAS.height}" role="img" aria-label="Hearth &amp; Honey">`);
  writeFileSync(OUT + 'logo.svg', normalized);
  writeFileSync(OUT + 'logo-cream.svg', normalized.replaceAll('#C84C5A', '#FAF3E8'));
  console.log(`logo.svg: ${(Buffer.byteLength(normalized) / 1024).toFixed(1)} KB, wordmark ${w}x${h} on ${CANVAS.width}x${CANVAS.height}`);
}

// 2. Megan: 3:4 portraits and 1:1 squares, WebP + JPEG.
async function buildMegan() {
  const upright = await sharp(SRC + 'megan.jpg').rotate().toBuffer();
  const meta = await sharp(upright).metadata();
  for (const w of [480, 800]) {
    const h = Math.round((w * 4) / 3);
    await sharp(upright).resize(w, h, { fit: 'cover', position: 'attention' }).webp({ quality: 78 }).toFile(`${OUT}megan-portrait-${w}.webp`);
    await sharp(upright).resize(w, h, { fit: 'cover', position: 'attention' }).jpeg({ quality: 80, mozjpeg: true }).toFile(`${OUT}megan-portrait-${w}.jpg`);
  }
  const side = Math.min(meta.width, meta.height);
  const top = Math.round((meta.height - side) * 0.3); // keep the face, drop some sky
  for (const w of [300, 600]) {
    await sharp(upright).extract({ left: 0, top, width: side, height: side }).resize(w, w).webp({ quality: 78 }).toFile(`${OUT}megan-square-${w}.webp`);
    await sharp(upright).extract({ left: 0, top, width: side, height: side }).resize(w, w).jpeg({ quality: 80, mozjpeg: true }).toFile(`${OUT}megan-square-${w}.jpg`);
  }
  console.log(`megan: source ${meta.width}x${meta.height}, square crop from y=${top}`);
}

// 3. Roll PNG (for schema/social), Open Graph image, favicons.
async function buildRollOgIcons() {
  const rollSvg = readFileSync(OUT + 'cinnamon-roll.svg');
  const rollBig = await sharp(rollSvg, { density: 300 }).resize(900).png().toBuffer();
  await sharp({ create: { width: 1200, height: 900, channels: 4, background: '#FAF3E8' } })
    .composite([{ input: rollBig, gravity: 'centre' }]).png().toFile(OUT + 'cinnamon-roll-1200.png');

  const logo = await sharp(readFileSync(OUT + 'logo.svg'), { density: 300 }).resize(640).png().toBuffer();
  const rollSmall = await sharp(rollSvg, { density: 300 }).resize(400).png().toBuffer();
  const base = Buffer.from(`<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="630">
    <rect width="1200" height="630" fill="#FAF3E8"/>
    <rect y="606" width="1200" height="24" fill="#C84C5A"/>
    <text x="80" y="452" font-family="Georgia, 'Times New Roman', serif" font-size="36" fill="#3B2A22">Hand-rolled cinnamon rolls, baked in Draper, Utah</text>
    <text x="80" y="506" font-family="Verdana, Arial, sans-serif" font-size="24" fill="#A93A48">orders@hearthhoneybakery.com   ·   @hearth.honey.bakery</text>
  </svg>`);
  await sharp(base).composite([{ input: logo, left: 60, top: 120 }, { input: rollSmall, left: 740, top: 140 }]).png().toFile(OUT + 'og-image.png');

  const fav = readFileSync(OUT + 'favicon.svg');
  const png = (size) => sharp(fav, { density: 400 }).resize(size, size).png();
  for (const [name, size] of [['favicon-32.png', 32], ['apple-touch-icon.png', 180], ['icon-192.png', 192], ['icon-512.png', 512]]) {
    await png(size).toFile(OUT + name);
  }
  const ico = await pngToIco([await png(16).toBuffer(), await png(32).toBuffer(), await png(48).toBuffer()]);
  writeFileSync(ROOT + 'favicon.ico', ico);
  console.log('roll PNG, og-image.png, favicons written');
}

await buildLogo();
await buildMegan();
await buildRollOgIcons();
```

- [ ] **Step 2: Run it and inspect the outputs**

Run: `cd tools && npm run build:images`
Expected: three log lines, no errors. Then render the traced logo and view it together with the OG image:
`node -e "import('sharp').then(async({default:s})=>{await s('../assets/img/logo.svg',{density:150}).flatten({background:'#FAF3E8'}).resize(1000).png().toFile('reports/logo-preview.png');console.log('ok')})"`
View `tools/reports/logo-preview.png`, `assets/img/og-image.png`, `assets/img/megan-square-600.jpg`, `assets/img/megan-portrait-480.jpg` with the Read tool.
Expected: crisp wordmark with clean ampersand (if edges are lumpy, lower `optTolerance` to 0.2 or raise `threshold` to 180 and re-run); OG image with logo left, roll right, tagline legible (if the text did not render, remove both `<text>` lines and re-run); Megan's face fully inside the square crop.

- [ ] **Step 3: Commit**

```bash
git add tools/build-images.mjs assets/img favicon.ico
git commit -m "feat: processed logo, photos, social image, and icons"
```

---

### Task 5: Self-hosted fonts

**Files:**
- Create: `tools/fetch-fonts.mjs`
- Generates: `assets/fonts/fraunces-latin.woff2`, `assets/fonts/fraunces-italic-latin.woff2`, `assets/fonts/figtree-latin.woff2`

**Interfaces:**
- Produces: the three woff2 files referenced by the `@font-face` rules in Task 7. Fraunces carries axes opsz, wght, SOFT; Figtree carries wght 300–900.

- [ ] **Step 1: Write the fetcher**

```js path=tools/fetch-fonts.mjs
// Downloads the latin-subset variable woff2 files from Google Fonts so the site serves them itself.
// Run once: npm run fetch:fonts  (the files are committed; re-run only to change families)
import { writeFileSync, mkdirSync } from 'node:fs';
import { fileURLToPath } from 'node:url';

const OUT = fileURLToPath(new URL('../assets/fonts/', import.meta.url));
mkdirSync(OUT, { recursive: true });
const UA = 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/128.0.0.0 Safari/537.36';

const FAMILIES = [
  { query: 'Fraunces:ital,opsz,wght,SOFT@0,9..144,100..900,0..100;1,9..144,100..900,0..100', files: { normal: 'fraunces-latin.woff2', italic: 'fraunces-italic-latin.woff2' } },
  { query: 'Figtree:wght@300..900', files: { normal: 'figtree-latin.woff2' } },
];

for (const fam of FAMILIES) {
  const css = await (await fetch(`https://fonts.googleapis.com/css2?family=${fam.query}&display=swap`, { headers: { 'User-Agent': UA } })).text();
  const blocks = css.matchAll(/\/\* (?<subset>[\w-]+) \*\/\s*@font-face\s*\{(?<body>[^}]+)\}/g);
  for (const { groups: { subset, body } } of blocks) {
    if (subset !== 'latin') continue;
    const style = /font-style:\s*(\w+)/.exec(body)[1];
    const url = /url\((https:[^)]+\.woff2)\)/.exec(body)[1];
    const name = fam.files[style];
    if (!name) continue;
    const buf = Buffer.from(await (await fetch(url)).arrayBuffer());
    writeFileSync(OUT + name, buf);
    console.log(`${name}: ${(buf.length / 1024).toFixed(1)} KB`);
  }
}
```

- [ ] **Step 2: Run it**

Run: `cd tools && npm run fetch:fonts`
Expected: three lines, each under 150 KB. If the network is unavailable, stop and report; the CSS in Task 7 falls back to system fonts but the preload links would 404, so do not proceed to Task 7 without the files.

- [ ] **Step 3: Commit**

```bash
git add tools/fetch-fonts.mjs assets/fonts
git commit -m "feat: self-hosted Fraunces and Figtree"
```

---

### Task 6: Order helpers with unit tests

**Files:**
- Create: `assets/js/order-mail.js`, `tools/tests/order-mail.test.mjs`

**Interfaces:**
- Produces: `ORDER_EMAIL`, `LEAD_DAYS`, `ITEMS` (slug → label), `minOrderDate(today?, leadDays?) → 'YYYY-MM-DD'`, `formatDate(iso) → 'Saturday, September 12, 2026'`, `validateOrder(fields, today?) → { [field]: message }`, `buildOrderMail(fields) → { subject, body, href }`. Field names: `name, email, item, quantity, date, instagram, notes`. Task 8 (`order.js`) and Task 12 (form ids) use these names exactly.

- [ ] **Step 1: Write the failing tests**

```js path=tools/tests/order-mail.test.mjs
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { ORDER_EMAIL, minOrderDate, formatDate, validateOrder, buildOrderMail } from '../../assets/js/order-mail.js';

const today = new Date(2026, 8, 6); // Sunday, September 6, 2026 (local time)
const good = { name: 'Sam Lee', email: 'sam@example.com', item: 'cinnamon-rolls', quantity: '12', date: '2026-09-12', instagram: 'samlee', notes: 'Birthday!' };

test('minOrderDate adds the lead time in local time and rolls over months', () => {
  assert.equal(minOrderDate(today, 2), '2026-09-08');
  assert.equal(minOrderDate(new Date(2026, 11, 31), 2), '2027-01-02');
});

test('formatDate renders a local date without timezone drift', () => {
  assert.equal(formatDate('2026-09-12'), 'Saturday, September 12, 2026');
});

test('validateOrder accepts a complete order', () => {
  assert.deepEqual(validateOrder(good, today), {});
});

test('validateOrder flags every missing required field', () => {
  const errors = validateOrder({ item: 'nope' }, today);
  assert.deepEqual(Object.keys(errors).sort(), ['date', 'email', 'item', 'name', 'quantity']);
});

test('validateOrder rejects a bad email, zero quantity, and a too-soon date', () => {
  const errors = validateOrder({ ...good, email: 'sam@', quantity: '0', date: '2026-09-07' }, today);
  assert.match(errors.email, /typo/);
  assert.match(errors.quantity, /1 or more/);
  assert.match(errors.date, /September 8, 2026/);
});

test('buildOrderMail composes subject, body, and mailto href', () => {
  const m = buildOrderMail(good);
  assert.equal(m.subject, 'Order request: Cinnamon Rolls × 12 for Saturday, September 12, 2026');
  assert.match(m.body, /Item: Cinnamon Rolls\nQuantity: 12/);
  assert.match(m.body, /Instagram: @samlee/);
  assert.match(m.body, /Notes:\nBirthday!/);
  assert.ok(m.href.startsWith(`mailto:${ORDER_EMAIL}?subject=`));
  assert.ok(decodeURIComponent(m.href).includes('Sam Lee'));
});

test('buildOrderMail omits Instagram when blank and writes (none) for empty notes', () => {
  const m = buildOrderMail({ ...good, instagram: '', notes: '' });
  assert.doesNotMatch(m.body, /Instagram:/);
  assert.match(m.body, /Notes:\n\(none\)/);
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd tools && npm test`
Expected: FAIL, "Cannot find module ... order-mail.js".

- [ ] **Step 3: Write the module**

```js path=assets/js/order-mail.js
// Pure helpers for the order form. No DOM here, so Node can unit-test them (tools/tests).
export const ORDER_EMAIL = 'orders@hearthhoneybakery.com';
export const LEAD_DAYS = 2; // Minimum notice in days. The FAQ on /order/ states the same number.
export const ITEMS = {
  'cinnamon-rolls': 'Cinnamon Rolls',
  'something-else': 'Something else',
};

const pad = (n) => String(n).padStart(2, '0');

/** Local-date ISO string (YYYY-MM-DD) for `today` plus `leadDays`. */
export function minOrderDate(today = new Date(), leadDays = LEAD_DAYS) {
  const d = new Date(today.getFullYear(), today.getMonth(), today.getDate() + leadDays);
  return `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())}`;
}

/** 'YYYY-MM-DD' -> 'Saturday, September 12, 2026', parsed as a local date so it never shifts a day. */
export function formatDate(iso) {
  const [y, m, d] = iso.split('-').map(Number);
  return new Date(y, m - 1, d).toLocaleDateString('en-US', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' });
}

/** Returns { field: message } for every problem. An empty object means the order is valid. */
export function validateOrder(f, today = new Date()) {
  const errors = {};
  if (!f.name || !f.name.trim()) errors.name = 'Please tell us your name.';
  if (!f.email || !f.email.trim()) errors.email = 'Please add your email so Megan can reply.';
  else if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(f.email.trim())) errors.email = 'That email doesn’t look right. Check for a typo?';
  if (!ITEMS[f.item]) errors.item = 'Please pick something from the list.';
  const qty = Number(f.quantity);
  if (!f.quantity || !Number.isInteger(qty) || qty < 1) errors.quantity = 'Please enter a whole number, 1 or more.';
  if (!f.date) errors.date = 'Please pick the day you’d like your order.';
  else if (!/^\d{4}-\d{2}-\d{2}$/.test(f.date)) errors.date = 'Please enter the date as YYYY-MM-DD.';
  else if (f.date < minOrderDate(today)) errors.date = `Please give us at least ${LEAD_DAYS} days’ notice. The earliest day is ${formatDate(minOrderDate(today))}.`;
  return errors;
}

/** Builds the email from validated fields. */
export function buildOrderMail(f) {
  const item = ITEMS[f.item];
  const quantity = Number(f.quantity);
  const when = formatDate(f.date);
  const handle = (f.instagram || '').trim().replace(/^@/, '');
  const subject = `Order request: ${item} × ${quantity} for ${when}`;
  const lines = [
    'Hi Megan,',
    '',
    'I’d like to place an order.',
    '',
    `Item: ${item}`,
    `Quantity: ${quantity}`,
    `Date wanted: ${when}`,
    '',
    `Name: ${f.name.trim()}`,
    `Email: ${f.email.trim()}`,
    handle ? `Instagram: @${handle}` : null,
    '',
    'Notes:',
    (f.notes || '').trim() || '(none)',
    '',
    'Thanks!',
  ].filter((line) => line !== null);
  const body = lines.join('\n');
  const href = `mailto:${ORDER_EMAIL}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;
  return { subject, body, href };
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd tools && npm test`
Expected: 7 passing, 0 failing.

- [ ] **Step 5: Commit**

```bash
git add assets/js/order-mail.js tools/tests/order-mail.test.mjs
git commit -m "feat: order email helpers with unit tests"
```

---
### Task 7: Stylesheet

Invoke `frontend-design:frontend-design` before this task and keep its guidance in mind while writing; the stylesheet below is the intended design and may be refined during the visual check in Task 14, never replaced with a generic look.

**Files:**
- Create: `assets/css/site.css`

**Interfaces:**
- Consumes: fonts from Task 5, `swirl.svg` from Task 3, tokens from Task 2.
- Produces: class names used by every page: `.skip-link .wrap .wrap--narrow .eyebrow .lede .section .section--band .section--tight .section__head .site-header .site-header__inner .brand .nav-toggle .site-nav .button .button--primary .button--secondary .button--light .cta-row .hero .hero__inner .hero__art .hero__steam .hero__badge .drip .grid .grid--2 .grid--3 .card .card__media .card__body .card__meta .card__actions .card--placeholder .swirl .steps .baker .baker__photo .band .band__inner .form .field .hint .error .form__row .error-summary .error-summary__title .status .copy-panel .order-form .order-fallback .faq .site-footer .site-footer__inner .site-footer__brand .site-footer__small .page-hero .about .about__photo .notes .not-found .visually-hidden`.

- [ ] **Step 1: Write the stylesheet**

```css path=assets/css/site.css
/* ==========================================================================
   Hearth & Honey · site.css
   Plain CSS, no build step. Edit, save, refresh.
   1 Fonts · 2 Tokens · 3 Base · 4 Utilities · 5 Header & nav · 6 Buttons
   7 Hero · 8 Cards & grids · 9 Steps · 10 Baker · 11 Bands · 12 Forms
   13 Footer · 14 Page extras · 15 Motion
   ========================================================================== */

/* 1 · Fonts (self-hosted, latin subset; see tools/fetch-fonts.mjs) */
@font-face {
  font-family: "Fraunces";
  src: url("/assets/fonts/fraunces-latin.woff2") format("woff2");
  font-weight: 100 900;
  font-style: normal;
  font-display: swap;
  unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+0304, U+0308, U+0329, U+2000-206F, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
}
@font-face {
  font-family: "Fraunces";
  src: url("/assets/fonts/fraunces-italic-latin.woff2") format("woff2");
  font-weight: 100 900;
  font-style: italic;
  font-display: swap;
  unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+0304, U+0308, U+0329, U+2000-206F, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
}
@font-face {
  font-family: "Figtree";
  src: url("/assets/fonts/figtree-latin.woff2") format("woff2");
  font-weight: 300 900;
  font-style: normal;
  font-display: swap;
  unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+0304, U+0308, U+0329, U+2000-206F, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
}

/* 2 · Tokens */
:root {
  color-scheme: light;
  --cream: #FAF3E8;
  --cream-2: #F3E6D3;
  --paper: #FFFCF7;
  --red: #C84C5A;
  --red-ink: #A93A48;
  --red-deep: #8E2C39;
  --ink: #3B2A22;
  --ink-soft: #6B5A50;
  --honey: #E3A93D;
  --line: rgb(59 42 34 / 0.14);
  --font-display: "Fraunces", Georgia, "Times New Roman", serif;
  --font-body: "Figtree", system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;
  --step--1: clamp(0.875rem, 0.85rem + 0.15vw, 0.95rem);
  --step-0: clamp(1rem, 0.95rem + 0.3vw, 1.125rem);
  --step-1: clamp(1.25rem, 1.15rem + 0.5vw, 1.5rem);
  --step-2: clamp(1.6rem, 1.4rem + 1vw, 2.1rem);
  --step-3: clamp(2.1rem, 1.7rem + 1.8vw, 3rem);
  --step-4: clamp(2.5rem, 1.9rem + 3vw, 4.25rem);
  --space-1: 0.5rem;
  --space-2: 1rem;
  --space-3: 1.5rem;
  --space-4: 2.5rem;
  --space-5: 4rem;
  --space-6: clamp(4rem, 8vw, 7rem);
  --radius: 1.5rem;
  --radius-sm: 0.875rem;
  --radius-pill: 999px;
  --shadow: 0 14px 36px -18px rgb(59 42 34 / 0.35);
  --wrap: 72rem;
  --gutter: clamp(1.25rem, 4vw, 2.5rem);
  --ease: cubic-bezier(0.2, 0.7, 0.2, 1);
}

/* 3 · Base */
*, *::before, *::after { box-sizing: border-box; }
html { -webkit-text-size-adjust: 100%; scroll-behavior: smooth; }
body {
  margin: 0;
  background: var(--cream);
  color: var(--ink);
  font-family: var(--font-body);
  font-size: var(--step-0);
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}
img, svg { display: block; max-width: 100%; height: auto; }
h1, h2, h3, h4 {
  font-family: var(--font-display);
  font-weight: 600;
  line-height: 1.08;
  letter-spacing: -0.01em;
  margin: 0 0 0.5em;
  font-variation-settings: "SOFT" 60, "opsz" 72;
  text-wrap: balance;
}
h1 { font-size: var(--step-4); color: var(--red); }
h2 { font-size: var(--step-3); color: var(--red); }
h3 { font-size: var(--step-1); color: var(--red-ink); font-variation-settings: "SOFT" 40, "opsz" 32; }
p { margin: 0 0 1em; max-width: 62ch; text-wrap: pretty; }
a {
  color: var(--red-ink);
  text-decoration: underline;
  text-decoration-thickness: 0.08em;
  text-underline-offset: 0.18em;
  transition: color 0.2s var(--ease);
}
a:hover { color: var(--red); }
:focus-visible { outline: 3px solid var(--red); outline-offset: 3px; border-radius: 4px; }
::selection { background: var(--red); color: #fff; }
ul, ol { padding-left: 1.25em; }
strong { font-weight: 700; }
em { font-family: var(--font-display); font-style: italic; font-variation-settings: "SOFT" 100, "opsz" 24; }
main:focus { outline: none; }
.visually-hidden {
  position: absolute !important;
  width: 1px; height: 1px;
  margin: -1px; padding: 0;
  overflow: hidden;
  clip: rect(0 0 0 0);
  clip-path: inset(50%);
  white-space: nowrap;
  border: 0;
}

/* 4 · Utilities */
.wrap { width: min(100% - 2 * var(--gutter), var(--wrap)); margin-inline: auto; }
.wrap--narrow { --wrap: 46rem; }
.eyebrow {
  display: inline-flex; align-items: center; gap: 0.5rem;
  margin: 0 0 var(--space-2);
  font-weight: 700; font-size: var(--step--1);
  letter-spacing: 0.08em; text-transform: uppercase;
  color: var(--red-ink);
}
.eyebrow::before, .swirl {
  content: "";
  display: inline-block;
  width: 1.1em; height: 1.1em;
  background: currentColor;
  -webkit-mask: url("/assets/img/swirl.svg") center / contain no-repeat;
  mask: url("/assets/img/swirl.svg") center / contain no-repeat;
}
.lede { font-size: var(--step-1); color: var(--ink-soft); line-height: 1.45; }
.section { position: relative; padding-block: var(--space-6); }
.section--tight { padding-block: var(--space-5); }
.section--band { background: var(--cream-2); }
.section__head { max-width: 46rem; margin-bottom: var(--space-4); }
.section__head p:last-child { margin-bottom: 0; }
.skip-link {
  position: absolute; top: 0; left: 0; z-index: 100;
  padding: 0.75rem 1rem;
  background: var(--red); color: #fff; font-weight: 700;
  border-radius: 0 0 var(--radius-sm) 0;
  transform: translateY(-120%);
  transition: transform 0.2s var(--ease);
}
.skip-link:focus { transform: none; outline-offset: -3px; outline-color: #fff; }

/* 5 · Header & nav */
.site-header {
  position: sticky; top: 0; z-index: 50;
  background: rgb(250 243 232 / 0.9);
  -webkit-backdrop-filter: blur(10px);
  backdrop-filter: blur(10px);
  border-bottom: 1px solid var(--line);
}
.site-header__inner {
  position: relative;
  display: flex; align-items: center; justify-content: space-between; gap: var(--space-2);
  min-height: 4.5rem;
}
.no-js .site-header__inner { flex-wrap: wrap; }
.brand { display: inline-flex; align-items: center; padding: 0.25rem 0; border-radius: var(--radius-sm); }
.brand img { width: 160px; height: 64px; }
.nav-toggle {
  display: inline-flex; align-items: center; justify-content: center;
  width: 2.75rem; height: 2.75rem;
  border: 2px solid var(--red); border-radius: var(--radius-pill);
  background: transparent; color: var(--red); cursor: pointer;
}
.nav-toggle svg { width: 1.375rem; height: 1.375rem; }
.nav-toggle[aria-expanded="true"] .icon-open,
.nav-toggle[aria-expanded="false"] .icon-close { display: none; }
.site-nav ul { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; gap: 0.25rem; }
.site-nav a {
  display: block; padding: 0.75rem 1rem;
  font-weight: 700; text-decoration: none; color: var(--ink);
  border-radius: var(--radius-sm);
}
.site-nav a:hover { color: var(--red); background: var(--paper); }
.site-nav a[aria-current="page"]:not(.button) { color: var(--red-ink); box-shadow: inset 0 -3px 0 var(--red); border-radius: 0; }
.site-nav .button { justify-content: center; margin-top: 0.5rem; color: #fff; }
.site-nav .button:hover { color: #fff; background: var(--red-deep); }
@media (max-width: 47.99em) {
  .js .site-nav {
    display: none;
    position: absolute; left: calc(-1 * var(--gutter)); right: calc(-1 * var(--gutter)); top: 100%;
    padding: var(--space-2) var(--gutter) var(--space-3);
    background: var(--cream);
    border-bottom: 1px solid var(--line);
    box-shadow: var(--shadow);
  }
  .js .site-nav.is-open { display: block; }
}
@media (min-width: 48em) {
  .nav-toggle { display: none; }
  .site-nav ul { flex-direction: row; align-items: center; gap: 0.25rem; }
  .site-nav .button { margin: 0 0 0 0.5rem; }
}
.no-js .nav-toggle { display: none; }

/* 6 · Buttons */
.button {
  display: inline-flex; align-items: center; justify-content: center; gap: 0.5rem;
  min-height: 2.75rem; padding: 0.7rem 1.4rem;
  border-radius: var(--radius-pill); border: 2px solid transparent;
  font: inherit; font-weight: 700; text-decoration: none; cursor: pointer;
  transition: transform 0.2s var(--ease), background-color 0.2s var(--ease), color 0.2s var(--ease), box-shadow 0.2s var(--ease);
}
.button:hover { transform: translateY(-2px); }
.button:active { transform: translateY(0) scale(0.98); }
.button--primary { background: var(--red); color: #fff; box-shadow: 0 8px 20px -10px rgb(200 76 90 / 0.7); }
.button--primary:hover { background: var(--red-deep); color: #fff; }
.button--secondary { background: transparent; color: var(--red-ink); border-color: var(--red); }
.button--secondary:hover { background: var(--paper); color: var(--red-deep); }
.button--light { background: #fff; color: var(--red-ink); }
.button--light:hover { background: var(--paper); color: var(--red-deep); }
.button svg { width: 1.25em; height: 1.25em; flex: none; }
.cta-row { display: flex; flex-wrap: wrap; gap: var(--space-2); align-items: center; }

/* 7 · Hero */
.hero { position: relative; background: var(--paper); padding-block: var(--space-5) calc(var(--space-6) + 1rem); }
.hero__inner { display: grid; gap: var(--space-4); align-items: center; }
.hero h1 { margin-bottom: 0.4em; }
.hero .lede { margin-bottom: var(--space-3); }
.hero__art { position: relative; width: min(100%, 22rem); margin: var(--space-3) auto 0; }
.hero__art > img { width: 100%; }
.hero__steam {
  position: absolute; left: 50%; top: -3.25rem;
  width: 6rem; height: 5rem;
  transform: translateX(-50%);
  color: var(--red); opacity: 0.55;
}
.hero__badge { position: absolute; right: -0.5rem; bottom: -0.75rem; width: 7.5rem; height: 7.5rem; color: var(--red); }
.hero__badge .badge-bg { fill: var(--cream); }
.hero__badge text { font-family: var(--font-body); font-weight: 700; font-size: 10.5px; letter-spacing: 0.16em; text-transform: uppercase; fill: currentColor; }
@media (min-width: 56em) {
  .hero__inner { grid-template-columns: 1.1fr 0.9fr; }
  .hero__art { width: min(100%, 26rem); margin-top: 0; }
}
.drip {
  position: absolute; left: 0; right: 0; bottom: 0; z-index: 1;
  width: 100%; height: 3.5rem;
  transform: translateY(100%);
  pointer-events: none;
}
.hero .drip { color: var(--paper); }

/* 8 · Cards & grids */
.grid { display: grid; gap: var(--space-3); }
@media (min-width: 40em) { .grid--2, .grid--3 { grid-template-columns: repeat(2, 1fr); } }
@media (min-width: 64em) { .grid--3 { grid-template-columns: repeat(3, 1fr); } }
.card {
  display: flex; flex-direction: column;
  background: var(--paper);
  border: 1px solid var(--line); border-radius: var(--radius);
  overflow: hidden;
  transition: transform 0.25s var(--ease), box-shadow 0.25s var(--ease);
}
.card:hover { transform: translateY(-4px); box-shadow: var(--shadow); }
.card__media { display: grid; place-items: center; aspect-ratio: 4 / 3; padding: var(--space-3); background: var(--cream-2); }
.card__media img { width: 72%; transition: transform 0.35s var(--ease); }
.card:hover .card__media img { transform: rotate(-4deg) scale(1.04); }
.card__body { display: flex; flex-direction: column; gap: var(--space-2); padding: var(--space-3); flex: 1; }
.card__body h3 { margin: 0; font-size: var(--step-2); color: var(--red); }
.card__body p { margin: 0; }
.card__meta { margin: 0; font-size: var(--step--1); color: var(--ink-soft); }
.card__meta dt { display: inline; font-weight: 700; }
.card__meta dd { display: inline; margin: 0; }
.card__actions { margin-top: auto; padding-top: var(--space-1); display: flex; flex-wrap: wrap; gap: var(--space-2); align-items: center; }
.card--placeholder {
  align-items: center; justify-content: center; text-align: center;
  gap: var(--space-2); padding: var(--space-4) var(--space-3);
  background: transparent;
  border: 2px dashed rgb(200 76 90 / 0.45);
}
.card--placeholder:hover { transform: none; box-shadow: none; border-color: var(--red); }
.card--placeholder .swirl { width: 3rem; height: 3rem; color: var(--red); opacity: 0.7; }
.card--placeholder h3 { margin: 0; font-size: var(--step-1); }
.card--placeholder p { margin: 0; color: var(--ink-soft); max-width: 28ch; }

/* 9 · Steps */
.steps { list-style: none; margin: var(--space-3) 0 0; padding: 0; display: grid; gap: var(--space-4) var(--space-3); counter-reset: step; }
@media (min-width: 48em) { .steps { grid-template-columns: repeat(3, 1fr); } }
.steps li {
  counter-increment: step;
  position: relative;
  padding: var(--space-5) var(--space-3) var(--space-3);
  background: var(--paper);
  border: 1px solid var(--line); border-radius: var(--radius);
}
.steps li::before {
  content: counter(step);
  position: absolute; top: -1.25rem; left: var(--space-3);
  display: grid; place-items: center;
  width: 3.25rem; height: 3.25rem;
  background: var(--red); color: #fff;
  font-family: var(--font-display); font-size: 1.6rem; font-weight: 700;
  font-variation-settings: "SOFT" 100;
  border-radius: 50%;
  box-shadow: 0 6px 14px -6px rgb(200 76 90 / 0.6);
  transform: rotate(-6deg);
}
.steps h3 { margin: 0 0 0.35em; }
.steps p { margin: 0; color: var(--ink-soft); }

/* 10 · Baker */
.baker { display: grid; gap: var(--space-4); align-items: center; }
@media (min-width: 48em) { .baker { grid-template-columns: minmax(14rem, 22rem) 1fr; } }
.baker__photo {
  width: min(100%, 20rem); margin-inline: auto;
  border-radius: 58% 42% 55% 45% / 45% 55% 45% 55%;
  overflow: hidden;
  border: 6px solid var(--paper);
  box-shadow: var(--shadow);
  transform: rotate(-2deg);
}
.baker__photo img { aspect-ratio: 1; object-fit: cover; }

/* 11 · Bands */
.band { position: relative; background: var(--red-deep); color: var(--cream); padding-block: var(--space-5); }
.band h2, .band p { color: var(--cream); }
.band .eyebrow { color: var(--cream); }
.band a:not(.button) { color: #fff; }
.band .drip { color: var(--red-deep); }
.band__inner { display: grid; gap: var(--space-3); align-items: center; }
.band__inner p:last-of-type { margin-bottom: 0; }
@media (min-width: 48em) { .band__inner { grid-template-columns: 1fr auto; } }

/* 12 · Forms */
.form { display: grid; gap: var(--space-3); }
.form__row { display: grid; gap: var(--space-3); }
@media (min-width: 40em) { .form__row { grid-template-columns: 1fr 1fr; } }
fieldset { border: 0; padding: 0; margin: 0; min-width: 0; }
legend { font-family: var(--font-display); font-size: var(--step-2); font-weight: 600; color: var(--red); padding: 0; margin-bottom: var(--space-2); }
.field { display: grid; gap: 0.35rem; }
.field label { font-weight: 700; }
.field .hint { margin: 0; font-size: var(--step--1); color: var(--ink-soft); }
.field input, .field select, .field textarea {
  width: 100%; min-height: 2.75rem;
  padding: 0.6rem 0.9rem;
  font: inherit; color: var(--ink);
  background: var(--paper);
  border: 2px solid rgb(59 42 34 / 0.3); border-radius: var(--radius-sm);
}
.field textarea { min-height: 7rem; resize: vertical; }
.field input:focus-visible, .field select:focus-visible, .field textarea:focus-visible { border-color: var(--red); outline-offset: 2px; }
.field [aria-invalid="true"] { border-color: var(--red); }
.field .error { margin: 0; font-size: var(--step--1); font-weight: 700; color: var(--red-ink); }
.field .error:not(:empty)::before { content: "! "; }
.error-summary { padding: var(--space-3); background: var(--paper); border: 2px solid var(--red); border-radius: var(--radius-sm); }
.error-summary__title { font-family: var(--font-body); font-size: var(--step-0); color: var(--red-ink); margin: 0 0 0.5rem; }
.error-summary ul { margin: 0; }
.status { margin: 0; min-height: 1.5em; font-weight: 700; color: var(--red-ink); }
.copy-panel { display: grid; gap: var(--space-2); padding: var(--space-3); background: var(--cream-2); border-radius: var(--radius-sm); }
.copy-panel p { margin: 0; }
.copy-panel textarea {
  width: 100%; min-height: 14rem; padding: 0.9rem;
  font: inherit; font-size: var(--step--1); color: var(--ink);
  background: var(--paper); border: 2px solid var(--line); border-radius: var(--radius-sm);
}
.no-js .order-form, .js .order-fallback { display: none; }
.faq { display: grid; gap: var(--space-3); }
.faq h3 { margin: 0 0 0.3em; }
.faq p { margin: 0; color: var(--ink-soft); }

/* 13 · Footer */
.site-footer { position: relative; background: var(--cream-2); padding: calc(var(--space-6) + 1rem) 0 var(--space-4); }
.site-footer__inner { display: grid; gap: var(--space-4); }
@media (min-width: 48em) {
  .site-footer__inner { grid-template-columns: 1.4fr 1fr 1fr; }
  .site-footer__small { grid-column: 1 / -1; }
}
.site-footer__brand img { width: 200px; height: 80px; margin-bottom: var(--space-2); }
.site-footer__brand p { color: var(--ink-soft); }
.site-footer h2 { font-family: var(--font-body); font-size: var(--step--1); letter-spacing: 0.08em; text-transform: uppercase; color: var(--red-ink); margin-bottom: var(--space-2); }
.site-footer ul { list-style: none; margin: 0; padding: 0; display: grid; gap: 0.5rem; }
.site-footer__small { display: grid; gap: 0.5rem; padding-top: var(--space-3); border-top: 1px solid var(--line); font-size: var(--step--1); color: var(--ink-soft); }
.site-footer__small p { margin: 0; max-width: none; }

/* 14 · Page extras */
.page-hero { padding-block: var(--space-5) var(--space-4); }
.page-hero p:last-child { margin-bottom: 0; }
.about { display: grid; gap: var(--space-4); align-items: start; }
@media (min-width: 48em) { .about { grid-template-columns: minmax(16rem, 24rem) 1fr; } }
.about__photo { border-radius: var(--radius); overflow: hidden; border: 6px solid var(--paper); box-shadow: var(--shadow); transform: rotate(-1.5deg); }
.about__photo img { aspect-ratio: 3 / 4; object-fit: cover; }
.notes { display: grid; gap: var(--space-2); padding: var(--space-3); background: var(--paper); border: 1px solid var(--line); border-radius: var(--radius); }
.notes p { margin: 0; }
.not-found { text-align: center; padding-block: var(--space-6); }
.not-found img { width: min(100%, 16rem); margin: 0 auto var(--space-3); transform: rotate(12deg); }
.not-found p { margin-inline: auto; }
.not-found .cta-row { justify-content: center; }

/* 15 · Motion */
@keyframes steam {
  0% { transform: translateY(0) scaleX(1); opacity: 0; }
  30% { opacity: 0.8; }
  100% { transform: translateY(-1.5rem) scaleX(1.2); opacity: 0; }
}
.hero__steam path { animation: steam 3.2s ease-in-out infinite; transform-box: fill-box; transform-origin: bottom center; }
.hero__steam path:nth-child(2) { animation-delay: 1.1s; }
.hero__steam path:nth-child(3) { animation-delay: 2.1s; }
@keyframes spin { to { transform: rotate(360deg); } }
.hero__badge { animation: spin 28s linear infinite; }
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after { animation: none !important; transition: none !important; }
  html { scroll-behavior: auto; }
  .hero__steam { opacity: 0.35; }
}
```

- [ ] **Step 2: Sanity-check the file parses** (no server yet)

Run: `cd tools && node -e "const c=require('fs').readFileSync('../assets/css/site.css','utf8');const o=(c.match(/\{/g)||[]).length,x=(c.match(/\}/g)||[]).length;console.log(o===x?'braces balanced':'UNBALANCED '+o+' vs '+x)"`
Expected: `braces balanced`.

- [ ] **Step 3: Commit**

```bash
git add assets/css/site.css
git commit -m "feat: site stylesheet"
```

---

### Task 8: Site scripts

**Files:**
- Create: `assets/js/site.js`, `assets/js/order.js`

**Interfaces:**
- Consumes: `order-mail.js` exports from Task 6.
- Produces: `site.js` expects `.nav-toggle[aria-controls="site-nav"]` and `#site-nav`. `order.js` expects `form#order-form` with controls named `name email item quantity date instagram notes`, each with `id="<name>"` and an empty `<p id="<name>-error" class="error">`, plus `#order-errors` (tabindex -1, hidden), `#order-status` (aria-live), `#order-copy` (hidden), `#order-copy-text` (textarea), `#order-copy-button`.

- [ ] **Step 1: Write `site.js`**

```js path=assets/js/site.js
// Site-wide behavior: the mobile navigation disclosure. Everything else is plain HTML and CSS.
const toggle = document.querySelector('.nav-toggle');
const nav = document.getElementById('site-nav');

if (toggle && nav) {
  const setOpen = (open) => {
    toggle.setAttribute('aria-expanded', String(open));
    nav.classList.toggle('is-open', open);
  };
  const isOpen = () => toggle.getAttribute('aria-expanded') === 'true';

  toggle.addEventListener('click', () => setOpen(!isOpen()));

  document.addEventListener('keydown', (event) => {
    if (event.key === 'Escape' && isOpen()) {
      setOpen(false);
      toggle.focus();
    }
  });

  document.addEventListener('click', (event) => {
    if (isOpen() && !nav.contains(event.target) && !toggle.contains(event.target)) setOpen(false);
  });

  const desktop = window.matchMedia('(min-width: 48em)');
  desktop.addEventListener('change', () => { if (desktop.matches) setOpen(false); });
}
```

- [ ] **Step 2: Write `order.js`**

```js path=assets/js/order.js
// Order form: validates, composes the email, opens the visitor's mail app, and offers a copy fallback.
import { ITEMS, ORDER_EMAIL, minOrderDate, validateOrder, buildOrderMail } from './order-mail.js';

const form = document.getElementById('order-form');

if (form) {
  const FIELDS = ['name', 'email', 'item', 'quantity', 'date', 'instagram', 'notes'];
  const control = (key) => form.elements.namedItem(key);
  const errorFor = (key) => document.getElementById(`${key}-error`);
  const summary = document.getElementById('order-errors');
  const status = document.getElementById('order-status');
  const panel = document.getElementById('order-copy');
  const copyText = document.getElementById('order-copy-text');
  const copyButton = document.getElementById('order-copy-button');

  // /order/?item=cinnamon-rolls preselects the item.
  const wanted = new URLSearchParams(window.location.search).get('item');
  if (wanted && ITEMS[wanted]) control('item').value = wanted;

  control('date').min = minOrderDate();

  const read = () => Object.fromEntries(FIELDS.map((key) => [key, control(key).value]));

  function render(errors) {
    for (const key of FIELDS) {
      const message = errors[key] || '';
      errorFor(key).textContent = message;
      control(key).setAttribute('aria-invalid', message ? 'true' : 'false');
    }
    const keys = Object.keys(errors);
    if (keys.length === 0) {
      summary.hidden = true;
      summary.innerHTML = '';
      return;
    }
    summary.innerHTML =
      `<h2 class="error-summary__title">Please fix ${keys.length === 1 ? 'this' : `these ${keys.length} things`} before we open your email:</h2>` +
      `<ul>${keys.map((key) => `<li><a href="#${key}">${errors[key]}</a></li>`).join('')}</ul>`;
    summary.hidden = false;
    summary.focus();
  }

  form.addEventListener('submit', (event) => {
    event.preventDefault();
    const data = read();
    const errors = validateOrder(data);
    render(errors);
    if (Object.keys(errors).length > 0) return;

    const mail = buildOrderMail(data);
    copyText.value = `To: ${ORDER_EMAIL}\nSubject: ${mail.subject}\n\n${mail.body}`;
    panel.hidden = false;
    status.textContent = 'Your email app should be opening with the order filled in. If nothing happened, copy the details below and send them yourself.';
    window.location.href = mail.href;
  });

  // Clear a field's error as soon as it is corrected.
  form.addEventListener('input', (event) => {
    const key = event.target.name;
    if (!FIELDS.includes(key) || control(key).getAttribute('aria-invalid') !== 'true') return;
    if (!validateOrder(read())[key]) {
      control(key).setAttribute('aria-invalid', 'false');
      errorFor(key).textContent = '';
    }
  });

  copyButton.addEventListener('click', async () => {
    try {
      await navigator.clipboard.writeText(copyText.value);
      copyButton.textContent = 'Copied!';
    } catch {
      copyText.select();
      copyButton.textContent = 'Press Ctrl+C or Cmd+C to copy';
    }
    setTimeout(() => { copyButton.textContent = 'Copy order details'; }, 2500);
  });
}
```

- [ ] **Step 3: Syntax-check both files**

Run: `node --check assets/js/site.js && node --check assets/js/order.js && echo OK`
Expected: `OK`.

- [ ] **Step 4: Commit**

```bash
git add assets/js/site.js assets/js/order.js
git commit -m "feat: navigation and order form scripts"
```

---
### Task 9: Home page

**Files:**
- Create: `index.html`

**Interfaces:**
- Consumes: CSS classes from Task 7, `site.js` from Task 8, images from Tasks 3–4, fonts from Task 5.
- Produces: the canonical header and footer markup. Tasks 10–13 repeat it verbatim, changing only the `aria-current="page"` attribute so it sits on the link for the current page.

- [ ] **Step 1: Write the page**

```html path=index.html
<!DOCTYPE html>
<html lang="en-US" class="no-js">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<script>document.documentElement.className = document.documentElement.className.replace("no-js", "js");</script>
<title>Cinnamon Rolls in Draper, Utah | Hearth &amp; Honey Bakery</title>
<meta name="description" content="Hand-rolled cinnamon rolls from Hearth &amp; Honey, Megan Jackman's small-batch home bakery in Draper, Utah. Order by email or Instagram for meet-up pickup.">
<link rel="canonical" href="https://hearthhoneybakery.com/">
<meta name="robots" content="index, follow, max-image-preview:large">
<meta name="theme-color" content="#C84C5A">
<meta name="author" content="Megan Jackman">
<meta name="geo.region" content="US-UT">
<meta name="geo.placename" content="Draper, Utah">
<meta property="og:type" content="website">
<meta property="og:site_name" content="Hearth &amp; Honey Bakery">
<meta property="og:locale" content="en_US">
<meta property="og:title" content="Cinnamon Rolls in Draper, Utah | Hearth &amp; Honey Bakery">
<meta property="og:description" content="Hand-rolled cinnamon rolls from Megan Jackman's small-batch home bakery in Draper, Utah. Order by email or Instagram for meet-up pickup.">
<meta property="og:url" content="https://hearthhoneybakery.com/">
<meta property="og:image" content="https://hearthhoneybakery.com/assets/img/og-image.png">
<meta property="og:image:width" content="1200">
<meta property="og:image:height" content="630">
<meta property="og:image:alt" content="Hearth &amp; Honey wordmark beside an illustrated cinnamon roll">
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="Cinnamon Rolls in Draper, Utah | Hearth &amp; Honey Bakery">
<meta name="twitter:description" content="Hand-rolled cinnamon rolls from Megan Jackman's small-batch home bakery in Draper, Utah. Order by email or Instagram for meet-up pickup.">
<meta name="twitter:image" content="https://hearthhoneybakery.com/assets/img/og-image.png">
<link rel="icon" href="/favicon.ico" sizes="32x32">
<link rel="icon" href="/assets/img/favicon.svg" type="image/svg+xml">
<link rel="apple-touch-icon" href="/assets/img/apple-touch-icon.png">
<link rel="manifest" href="/site.webmanifest">
<link rel="preload" href="/assets/fonts/fraunces-latin.woff2" as="font" type="font/woff2" crossorigin>
<link rel="preload" href="/assets/fonts/figtree-latin.woff2" as="font" type="font/woff2" crossorigin>
<link rel="stylesheet" href="/assets/css/site.css">
<script type="module" src="/assets/js/site.js"></script>
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "Bakery",
      "@id": "https://hearthhoneybakery.com/#bakery",
      "name": "Hearth & Honey Bakery",
      "alternateName": ["Hearth & Honey", "Hearth and Honey"],
      "description": "Small-batch home bakery in Draper, Utah, known for hand-rolled cinnamon rolls. Order by email or Instagram DM; orders are handed off at a public meet-up spot around Draper.",
      "url": "https://hearthhoneybakery.com/",
      "email": "orders@hearthhoneybakery.com",
      "logo": { "@type": "ImageObject", "url": "https://hearthhoneybakery.com/assets/img/logo-600.png", "width": 600, "height": 240 },
      "image": ["https://hearthhoneybakery.com/assets/img/og-image.png", "https://hearthhoneybakery.com/assets/img/cinnamon-roll-1200.png"],
      "address": { "@type": "PostalAddress", "addressLocality": "Draper", "addressRegion": "UT", "postalCode": "84020", "addressCountry": "US" },
      "areaServed": { "@type": "City", "name": "Draper", "sameAs": "https://en.wikipedia.org/wiki/Draper,_Utah" },
      "sameAs": ["https://www.instagram.com/hearth.honey.bakery/"],
      "founder": { "@id": "https://hearthhoneybakery.com/#megan" },
      "servesCuisine": "Baked goods",
      "hasMenu": { "@id": "https://hearthhoneybakery.com/menu/#menu" },
      "potentialAction": { "@type": "OrderAction", "target": "https://hearthhoneybakery.com/order/", "deliveryMethod": "http://purl.org/goodrelations/v1#DeliveryModePickUp" }
    },
    {
      "@type": "Person",
      "@id": "https://hearthhoneybakery.com/#megan",
      "name": "Megan Jackman",
      "jobTitle": "Owner and baker",
      "worksFor": { "@id": "https://hearthhoneybakery.com/#bakery" },
      "image": "https://hearthhoneybakery.com/assets/img/megan-portrait-800.jpg",
      "url": "https://hearthhoneybakery.com/about/"
    },
    {
      "@type": "WebSite",
      "@id": "https://hearthhoneybakery.com/#website",
      "url": "https://hearthhoneybakery.com/",
      "name": "Hearth & Honey Bakery",
      "publisher": { "@id": "https://hearthhoneybakery.com/#bakery" },
      "inLanguage": "en-US"
    },
    {
      "@type": "WebPage",
      "@id": "https://hearthhoneybakery.com/#webpage",
      "url": "https://hearthhoneybakery.com/",
      "name": "Cinnamon Rolls in Draper, Utah | Hearth & Honey Bakery",
      "description": "Hand-rolled cinnamon rolls from Hearth & Honey, Megan Jackman's small-batch home bakery in Draper, Utah. Order by email or Instagram for meet-up pickup.",
      "isPartOf": { "@id": "https://hearthhoneybakery.com/#website" },
      "about": { "@id": "https://hearthhoneybakery.com/#bakery" },
      "primaryImageOfPage": "https://hearthhoneybakery.com/assets/img/og-image.png",
      "inLanguage": "en-US",
      "datePublished": "2026-09-06",
      "dateModified": "2026-09-06"
    },
    {
      "@type": "BreadcrumbList",
      "@id": "https://hearthhoneybakery.com/#breadcrumb",
      "itemListElement": [
        { "@type": "ListItem", "position": 1, "name": "Home", "item": "https://hearthhoneybakery.com/" }
      ]
    },
    {
      "@type": "Menu",
      "@id": "https://hearthhoneybakery.com/menu/#menu",
      "name": "Hearth & Honey menu",
      "url": "https://hearthhoneybakery.com/menu/",
      "hasMenuSection": {
        "@type": "MenuSection",
        "name": "Sweet bakes",
        "hasMenuItem": [
          {
            "@type": "MenuItem",
            "name": "Cinnamon Rolls",
            "description": "Soft, pillowy dough rolled up with brown sugar and cinnamon, baked golden and finished with a vanilla-cream glaze. Contains wheat, milk, and eggs.",
            "image": "https://hearthhoneybakery.com/assets/img/cinnamon-roll-1200.png",
            "url": "https://hearthhoneybakery.com/menu/#cinnamon-rolls"
          }
        ]
      }
    },
    {
      "@type": "Product",
      "@id": "https://hearthhoneybakery.com/menu/#cinnamon-rolls-product",
      "name": "Cinnamon Rolls",
      "description": "Hand-rolled cinnamon rolls baked to order in Draper, Utah. Soft dough, brown-sugar cinnamon filling, vanilla-cream glaze. Contains wheat, milk, and eggs.",
      "image": "https://hearthhoneybakery.com/assets/img/cinnamon-roll-1200.png",
      "brand": { "@type": "Brand", "name": "Hearth & Honey" },
      "category": "Baked goods",
      "url": "https://hearthhoneybakery.com/menu/#cinnamon-rolls",
      "manufacturer": { "@id": "https://hearthhoneybakery.com/#bakery" }
    }
  ]
}
</script>
</head>
<body>
<a class="skip-link" href="#main">Skip to main content</a>

<header class="site-header">
  <div class="wrap site-header__inner">
    <a class="brand" href="/"><img src="/assets/img/logo.svg" alt="Hearth &amp; Honey" width="160" height="64"></a>
    <button class="nav-toggle" type="button" aria-expanded="false" aria-controls="site-nav">
      <svg class="icon-open" viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M4 7h16M4 12h16M4 17h16"/></svg>
      <svg class="icon-close" viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M6 6l12 12M18 6 6 18"/></svg>
      <span class="visually-hidden">Site navigation</span>
    </button>
    <nav id="site-nav" class="site-nav" aria-label="Site">
      <ul>
        <li><a href="/" aria-current="page">Home</a></li>
        <li><a href="/menu/">Menu</a></li>
        <li><a href="/about/">Meet Megan</a></li>
        <li><a class="button button--primary" href="/order/">Order</a></li>
      </ul>
    </nav>
  </div>
</header>

<main id="main" tabindex="-1">

  <section class="hero" aria-labelledby="hero-title">
    <div class="wrap hero__inner">
      <div>
        <p class="eyebrow">Home-baked in Draper, Utah</p>
        <h1 id="hero-title">Hand-rolled cinnamon rolls, fresh from a Draper kitchen.</h1>
        <p class="lede">Hearth &amp; Honey is Megan Jackman's small-batch home bakery. Tell us how many you'd like, and we'll hand them over warm at a meet-up spot around Draper.</p>
        <div class="cta-row">
          <a class="button button--primary" href="/order/">
            <svg viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="5" width="18" height="14" rx="3"/><path d="m3 8 9 6 9-6"/></svg>
            Start an order
          </a>
          <a class="button button--secondary" href="https://ig.me/m/hearth.honey.bakery">
            <svg viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><rect x="3" y="3" width="18" height="18" rx="5"/><circle cx="12" cy="12" r="4"/><circle cx="17.5" cy="6.5" r="1" fill="currentColor" stroke="none"/></svg>
            Message on Instagram
          </a>
        </div>
      </div>
      <div class="hero__art">
        <svg class="hero__steam" viewBox="0 0 96 80" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round">
          <path d="M28 72c-8-10 8-18 0-30 6-6 4-12-2-16"/>
          <path d="M48 76c-8-10 8-18 0-30 6-6 4-12-2-16"/>
          <path d="M68 72c-8-10 8-18 0-30 6-6 4-12-2-16"/>
        </svg>
        <img src="/assets/img/cinnamon-roll.svg" alt="Illustrated cinnamon roll with glaze dripping down the sides" width="320" height="250" fetchpriority="high">
        <svg class="hero__badge" viewBox="0 0 120 120" aria-hidden="true">
          <circle class="badge-bg" cx="60" cy="60" r="58"/>
          <circle cx="60" cy="60" r="58" fill="none" stroke="currentColor" stroke-width="2"/>
          <defs><path id="badge-ring" d="M60 60m-44 0a44 44 0 1 1 88 0a44 44 0 1 1-88 0"/></defs>
          <text><textPath href="#badge-ring">Fresh baked · to order · fresh baked · to order ·</textPath></text>
          <image href="/assets/img/cinnamon-roll.svg" x="33" y="38" width="54" height="42"/>
        </svg>
      </div>
    </div>
    <svg class="drip" viewBox="0 0 1440 60" preserveAspectRatio="none" aria-hidden="true"><path fill="currentColor" d="M0 0H1440V22C1400 22 1392 50 1362 50C1332 50 1326 22 1290 22H1080C1050 22 1044 58 1010 58C976 58 970 22 930 22H730C700 22 694 42 668 42C642 42 636 22 596 22H380C350 22 344 54 310 54C276 54 270 22 230 22H0Z"/></svg>
  </section>

  <section class="section" aria-labelledby="baking-title">
    <div class="wrap">
      <div class="section__head">
        <p class="eyebrow">The menu, for now</p>
        <h2 id="baking-title">What's baking</h2>
        <p>One thing, made really well. Cinnamon rolls are the first bake on the menu, and every batch is made to order.</p>
      </div>
      <div class="grid grid--3">
        <article class="card" id="cinnamon-rolls">
          <div class="card__media">
            <!-- PHOTO SLOT: when you have a real photo, replace this <img> with it, e.g.
                 <img src="/assets/img/cinnamon-rolls-800.webp" alt="A tray of glazed cinnamon rolls" width="800" height="600" loading="lazy">
                 Keep width/height and write a real alt. -->
            <img src="/assets/img/cinnamon-roll.svg" alt="Illustrated cinnamon roll dripping with glaze" width="320" height="250" loading="lazy">
          </div>
          <div class="card__body">
            <h3>Cinnamon Rolls</h3>
            <p>Soft, pillowy dough rolled up with brown sugar and cinnamon, baked until the edges turn golden, then finished with a vanilla-cream glaze. Best eaten warm. They rarely survive the drive home.</p>
            <dl class="card__meta"><dt>Contains:</dt> <dd>wheat, milk, eggs</dd></dl>
            <div class="card__actions">
              <a class="button button--primary" href="/order/?item=cinnamon-rolls">Order cinnamon rolls</a>
              <a href="/menu/#cinnamon-rolls">Full ingredients</a>
            </div>
          </div>
        </article>
        <article class="card card--placeholder">
          <span class="swirl" aria-hidden="true"></span>
          <h3>Next bake: to be announced</h3>
          <p>Something new is proofing. Follow <a href="https://www.instagram.com/hearth.honey.bakery/">@hearth.honey.bakery</a> to hear about it first.</p>
        </article>
        <article class="card card--placeholder">
          <span class="swirl" aria-hidden="true"></span>
          <h3>Have something in mind?</h3>
          <p>Megan takes requests. <a href="/order/?item=something-else">Ask her</a> and she'll tell you if it's doable.</p>
        </article>
      </div>
    </div>
  </section>

  <section class="section section--band" aria-labelledby="how-title">
    <div class="wrap">
      <div class="section__head">
        <p class="eyebrow">Simple on purpose</p>
        <h2 id="how-title">How ordering works</h2>
        <p>No cart, no checkout, no account. You talk to the person who bakes your order.</p>
      </div>
      <ol class="steps">
        <li>
          <h3>Tell us what you'd like</h3>
          <p>Email <a href="mailto:orders@hearthhoneybakery.com">orders@hearthhoneybakery.com</a> or send a DM on Instagram with what you want, how many, and the day you'd like them. The <a href="/order/">order page</a> writes the email for you.</p>
        </li>
        <li>
          <h3>Megan bakes it fresh</h3>
          <p>She confirms your date and payment details, then bakes your order fresh for that day. Please give at least two days' notice.</p>
        </li>
        <li>
          <h3>Meet-up handoff</h3>
          <p>We meet at a public spot around Draper and hand over the goods, still warm if the timing works out. Bring napkins.</p>
        </li>
      </ol>
    </div>
  </section>

  <section class="section" aria-labelledby="baker-title">
    <div class="wrap baker">
      <div class="baker__photo">
        <picture>
          <source type="image/webp" srcset="/assets/img/megan-square-300.webp 300w, /assets/img/megan-square-600.webp 600w" sizes="(min-width: 48em) 20rem, 80vw">
          <img src="/assets/img/megan-square-600.jpg" srcset="/assets/img/megan-square-300.jpg 300w, /assets/img/megan-square-600.jpg 600w" sizes="(min-width: 48em) 20rem, 80vw" alt="Megan Jackman smiling outdoors in front of green trees" width="600" height="600" loading="lazy">
        </picture>
      </div>
      <div>
        <p class="eyebrow">Meet the baker</p>
        <h2 id="baker-title">Hi, I'm Megan.</h2>
        <p>I bake every order myself from my home kitchen in Draper. Small batches, real butter, and a glaze I take very seriously. If you order from Hearth &amp; Honey, you're talking to me.</p>
        <a class="button button--secondary" href="/about/">More about Megan</a>
      </div>
    </div>
  </section>

  <section class="band" aria-labelledby="ig-title">
    <div class="wrap band__inner">
      <div>
        <p class="eyebrow">On Instagram</p>
        <h2 id="ig-title">Come see the swirl.</h2>
        <p>Fresh bakes, behind-the-scenes, and the occasional flour explosion. New menu items show up there first.</p>
      </div>
      <a class="button button--light" href="https://www.instagram.com/hearth.honey.bakery/">
        <svg viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><rect x="3" y="3" width="18" height="18" rx="5"/><circle cx="12" cy="12" r="4"/><circle cx="17.5" cy="6.5" r="1" fill="currentColor" stroke="none"/></svg>
        Follow @hearth.honey.bakery
      </a>
    </div>
    <svg class="drip" viewBox="0 0 1440 60" preserveAspectRatio="none" aria-hidden="true"><path fill="currentColor" d="M0 0H1440V22C1400 22 1392 50 1362 50C1332 50 1326 22 1290 22H1080C1050 22 1044 58 1010 58C976 58 970 22 930 22H730C700 22 694 42 668 42C642 42 636 22 596 22H380C350 22 344 54 310 54C276 54 270 22 230 22H0Z"/></svg>
  </section>

</main>

<footer class="site-footer">
  <div class="wrap site-footer__inner">
    <div class="site-footer__brand">
      <img src="/assets/img/logo.svg" alt="Hearth &amp; Honey" width="200" height="80" loading="lazy">
      <p>Small-batch cinnamon rolls, baked at home in Draper, Utah, and handed over in person.</p>
    </div>
    <div>
      <h2>Order or say hi</h2>
      <ul>
        <li><a href="mailto:orders@hearthhoneybakery.com">orders@hearthhoneybakery.com</a></li>
        <li><a href="https://www.instagram.com/hearth.honey.bakery/">@hearth.honey.bakery</a></li>
        <li>Draper, Utah · meet-up pickup</li>
      </ul>
    </div>
    <div>
      <h2>Pages</h2>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/menu/">Menu</a></li>
        <li><a href="/about/">Meet Megan</a></li>
        <li><a href="/order/">Order</a></li>
      </ul>
    </div>
    <div class="site-footer__small">
      <p>Made in a home kitchen that is not licensed or inspected by the Utah Department of Agriculture and Food or a local health department.</p>
      <p>© 2026 Hearth &amp; Honey Bakery · Made with butter, cinnamon, and a lot of care. · <a href="/sitemap.xml">Sitemap</a></p>
    </div>
  </div>
</footer>
</body>
</html>
```

- [ ] **Step 2: Look at it**

Run: `cd tools && npm run serve` in the background, open `http://127.0.0.1:8080/` with Playwright and screenshot both widths:
`node -e "import('playwright-core').then(async({chromium})=>{const b=await chromium.launch({channel:'chrome'});for(const w of [390,1280]){const p=await b.newPage({viewport:{width:w,height:900}});await p.goto('http://127.0.0.1:8080/');await p.screenshot({path:'reports/home-'+w+'.png',fullPage:true});}await b.close();console.log('ok')})"`
View both PNGs. Expected: logo in header, hero with roll, steam above it, badge at lower right, drip under hero, three cards, numbered steps, Megan's blob photo, red band with drip, cream footer. Fix any layout breakage in `site.css` before continuing.

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "feat: home page"
```

---
### Task 10: Menu page

**Files:**
- Create: `menu/index.html`

**Interfaces:**
- Consumes: header/footer markup from Task 9 with `aria-current="page"` moved to the Menu link. Card markup identical to Task 9's product card plus an ingredients line. `id="cinnamon-rolls"` is the anchor linked from Home and from JSON-LD.

- [ ] **Step 1: Write the page**

```html path=menu/index.html
<!DOCTYPE html>
<html lang="en-US" class="no-js">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<script>document.documentElement.className = document.documentElement.className.replace("no-js", "js");</script>
<title>Menu: Cinnamon Rolls and More | Hearth &amp; Honey Bakery, Draper UT</title>
<meta name="description" content="See what's baking at Hearth &amp; Honey in Draper, Utah: hand-rolled cinnamon rolls made to order, with full ingredients and allergens. Order by email or Instagram.">
<link rel="canonical" href="https://hearthhoneybakery.com/menu/">
<meta name="robots" content="index, follow, max-image-preview:large">
<meta name="theme-color" content="#C84C5A">
<meta name="author" content="Megan Jackman">
<meta name="geo.region" content="US-UT">
<meta name="geo.placename" content="Draper, Utah">
<meta property="og:type" content="website">
<meta property="og:site_name" content="Hearth &amp; Honey Bakery">
<meta property="og:locale" content="en_US">
<meta property="og:title" content="Menu: Cinnamon Rolls and More | Hearth &amp; Honey Bakery">
<meta property="og:description" content="Hand-rolled cinnamon rolls made to order in Draper, Utah, with full ingredients and allergens. Order by email or Instagram.">
<meta property="og:url" content="https://hearthhoneybakery.com/menu/">
<meta property="og:image" content="https://hearthhoneybakery.com/assets/img/og-image.png">
<meta property="og:image:width" content="1200">
<meta property="og:image:height" content="630">
<meta property="og:image:alt" content="Hearth &amp; Honey wordmark beside an illustrated cinnamon roll">
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="Menu: Cinnamon Rolls and More | Hearth &amp; Honey Bakery">
<meta name="twitter:description" content="Hand-rolled cinnamon rolls made to order in Draper, Utah, with full ingredients and allergens. Order by email or Instagram.">
<meta name="twitter:image" content="https://hearthhoneybakery.com/assets/img/og-image.png">
<link rel="icon" href="/favicon.ico" sizes="32x32">
<link rel="icon" href="/assets/img/favicon.svg" type="image/svg+xml">
<link rel="apple-touch-icon" href="/assets/img/apple-touch-icon.png">
<link rel="manifest" href="/site.webmanifest">
<link rel="preload" href="/assets/fonts/fraunces-latin.woff2" as="font" type="font/woff2" crossorigin>
<link rel="preload" href="/assets/fonts/figtree-latin.woff2" as="font" type="font/woff2" crossorigin>
<link rel="stylesheet" href="/assets/css/site.css">
<script type="module" src="/assets/js/site.js"></script>
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "Bakery",
      "@id": "https://hearthhoneybakery.com/#bakery",
      "name": "Hearth & Honey Bakery",
      "alternateName": ["Hearth & Honey", "Hearth and Honey"],
      "description": "Small-batch home bakery in Draper, Utah, known for hand-rolled cinnamon rolls. Order by email or Instagram DM; orders are handed off at a public meet-up spot around Draper.",
      "url": "https://hearthhoneybakery.com/",
      "email": "orders@hearthhoneybakery.com",
      "logo": { "@type": "ImageObject", "url": "https://hearthhoneybakery.com/assets/img/logo-600.png", "width": 600, "height": 240 },
      "image": ["https://hearthhoneybakery.com/assets/img/og-image.png", "https://hearthhoneybakery.com/assets/img/cinnamon-roll-1200.png"],
      "address": { "@type": "PostalAddress", "addressLocality": "Draper", "addressRegion": "UT", "postalCode": "84020", "addressCountry": "US" },
      "areaServed": { "@type": "City", "name": "Draper", "sameAs": "https://en.wikipedia.org/wiki/Draper,_Utah" },
      "sameAs": ["https://www.instagram.com/hearth.honey.bakery/"],
      "founder": { "@id": "https://hearthhoneybakery.com/#megan" },
      "servesCuisine": "Baked goods",
      "hasMenu": { "@id": "https://hearthhoneybakery.com/menu/#menu" },
      "potentialAction": { "@type": "OrderAction", "target": "https://hearthhoneybakery.com/order/", "deliveryMethod": "http://purl.org/goodrelations/v1#DeliveryModePickUp" }
    },
    {
      "@type": "Person",
      "@id": "https://hearthhoneybakery.com/#megan",
      "name": "Megan Jackman",
      "jobTitle": "Owner and baker",
      "worksFor": { "@id": "https://hearthhoneybakery.com/#bakery" },
      "image": "https://hearthhoneybakery.com/assets/img/megan-portrait-800.jpg",
      "url": "https://hearthhoneybakery.com/about/"
    },
    {
      "@type": "WebSite",
      "@id": "https://hearthhoneybakery.com/#website",
      "url": "https://hearthhoneybakery.com/",
      "name": "Hearth & Honey Bakery",
      "publisher": { "@id": "https://hearthhoneybakery.com/#bakery" },
      "inLanguage": "en-US"
    },
    {
      "@type": "WebPage",
      "@id": "https://hearthhoneybakery.com/menu/#webpage",
      "url": "https://hearthhoneybakery.com/menu/",
      "name": "Menu: Cinnamon Rolls and More | Hearth & Honey Bakery, Draper UT",
      "description": "See what's baking at Hearth & Honey in Draper, Utah: hand-rolled cinnamon rolls made to order, with full ingredients and allergens. Order by email or Instagram.",
      "isPartOf": { "@id": "https://hearthhoneybakery.com/#website" },
      "about": { "@id": "https://hearthhoneybakery.com/#bakery" },
      "primaryImageOfPage": "https://hearthhoneybakery.com/assets/img/cinnamon-roll-1200.png",
      "inLanguage": "en-US",
      "datePublished": "2026-09-06",
      "dateModified": "2026-09-06"
    },
    {
      "@type": "BreadcrumbList",
      "@id": "https://hearthhoneybakery.com/menu/#breadcrumb",
      "itemListElement": [
        { "@type": "ListItem", "position": 1, "name": "Home", "item": "https://hearthhoneybakery.com/" },
        { "@type": "ListItem", "position": 2, "name": "Menu", "item": "https://hearthhoneybakery.com/menu/" }
      ]
    },
    {
      "@type": "Menu",
      "@id": "https://hearthhoneybakery.com/menu/#menu",
      "name": "Hearth & Honey menu",
      "url": "https://hearthhoneybakery.com/menu/",
      "hasMenuSection": {
        "@type": "MenuSection",
        "name": "Sweet bakes",
        "hasMenuItem": [
          {
            "@type": "MenuItem",
            "name": "Cinnamon Rolls",
            "description": "Soft, pillowy dough rolled up with brown sugar and cinnamon, baked golden and finished with a vanilla-cream glaze. Contains wheat, milk, and eggs.",
            "image": "https://hearthhoneybakery.com/assets/img/cinnamon-roll-1200.png",
            "url": "https://hearthhoneybakery.com/menu/#cinnamon-rolls"
          }
        ]
      }
    },
    {
      "@type": "Product",
      "@id": "https://hearthhoneybakery.com/menu/#cinnamon-rolls-product",
      "name": "Cinnamon Rolls",
      "description": "Hand-rolled cinnamon rolls baked to order in Draper, Utah. Soft dough, brown-sugar cinnamon filling, vanilla-cream glaze. Ingredients: flour, milk, brown sugar, heavy cream, butter, eggs, water, sugar, yeast, salt, vanilla, cinnamon. Contains wheat, milk, and eggs.",
      "image": "https://hearthhoneybakery.com/assets/img/cinnamon-roll-1200.png",
      "brand": { "@type": "Brand", "name": "Hearth & Honey" },
      "category": "Baked goods",
      "url": "https://hearthhoneybakery.com/menu/#cinnamon-rolls",
      "manufacturer": { "@id": "https://hearthhoneybakery.com/#bakery" }
    }
  ]
}
</script>
</head>
<body>
<a class="skip-link" href="#main">Skip to main content</a>

<header class="site-header">
  <div class="wrap site-header__inner">
    <a class="brand" href="/"><img src="/assets/img/logo.svg" alt="Hearth &amp; Honey" width="160" height="64"></a>
    <button class="nav-toggle" type="button" aria-expanded="false" aria-controls="site-nav">
      <svg class="icon-open" viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M4 7h16M4 12h16M4 17h16"/></svg>
      <svg class="icon-close" viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M6 6l12 12M18 6 6 18"/></svg>
      <span class="visually-hidden">Site navigation</span>
    </button>
    <nav id="site-nav" class="site-nav" aria-label="Site">
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/menu/" aria-current="page">Menu</a></li>
        <li><a href="/about/">Meet Megan</a></li>
        <li><a class="button button--primary" href="/order/">Order</a></li>
      </ul>
    </nav>
  </div>
</header>

<main id="main" tabindex="-1">

  <div class="wrap page-hero">
    <p class="eyebrow">Menu</p>
    <h1>What's baking</h1>
    <p class="lede">Everything here is made to order in small batches. Pick what you'd like, then send an order by email or Instagram and Megan will confirm a date.</p>
  </div>

  <section class="section section--tight" aria-label="Bakes you can order">
    <div class="wrap">
      <!-- HOW TO ADD A PRODUCT
           1. Copy the <article class="card" id="..."> block below and paste it after it.
           2. Change the id (lowercase, hyphens), the heading, the description, and the Contains/Made with lines.
           3. Change the Order link to /order/?item=<your-id>, then add the same id and label to ITEMS in /assets/js/order-mail.js
              and a matching <option> in /order/index.html.
           4. Optional: replace the illustration with a real photo (see the PHOTO SLOT comment).
           5. Copy the finished card onto the home page too if you want it featured there.
           6. Remove one of the placeholder cards below so the grid stays balanced. -->
      <div class="grid grid--3">
        <article class="card" id="cinnamon-rolls">
          <div class="card__media">
            <!-- PHOTO SLOT: when you have a real photo, replace this <img> with it, e.g.
                 <img src="/assets/img/cinnamon-rolls-800.webp" alt="A tray of glazed cinnamon rolls" width="800" height="600" loading="lazy">
                 Keep width/height and write a real alt. -->
            <img src="/assets/img/cinnamon-roll.svg" alt="Illustrated cinnamon roll dripping with glaze" width="320" height="250" fetchpriority="high">
          </div>
          <div class="card__body">
            <h2>Cinnamon Rolls</h2>
            <p>Soft, pillowy dough rolled up with brown sugar and cinnamon, baked until the edges turn golden, then finished with a vanilla-cream glaze. Best eaten warm. They rarely survive the drive home.</p>
            <dl class="card__meta">
              <div><dt>Made with:</dt> <dd>flour, milk, brown sugar, heavy cream, butter, eggs, water, sugar, yeast, salt, vanilla, cinnamon</dd></div>
              <div><dt>Contains:</dt> <dd>wheat, milk, eggs</dd></div>
            </dl>
            <div class="card__actions">
              <a class="button button--primary" href="/order/?item=cinnamon-rolls">Order cinnamon rolls</a>
            </div>
          </div>
        </article>
        <article class="card card--placeholder">
          <span class="swirl" aria-hidden="true"></span>
          <h2>Next bake: to be announced</h2>
          <p>Something new is proofing. Follow <a href="https://www.instagram.com/hearth.honey.bakery/">@hearth.honey.bakery</a> to hear about it first.</p>
        </article>
        <article class="card card--placeholder">
          <span class="swirl" aria-hidden="true"></span>
          <h2>Have something in mind?</h2>
          <p>Megan takes requests. <a href="/order/?item=something-else">Ask her</a> and she'll tell you if it's doable.</p>
        </article>
      </div>
    </div>
  </section>

  <section class="section section--tight" aria-labelledby="notes-title">
    <div class="wrap wrap--narrow">
      <h2 id="notes-title">Good to know</h2>
      <div class="notes">
        <p><strong>Allergens.</strong> Cinnamon rolls contain wheat, milk, and eggs. Everything is made in a home kitchen that also handles nuts and other allergens, so it is not a safe choice for severe allergies.</p>
        <p><strong>Made to order.</strong> Nothing sits on a shelf. Please give at least two days' notice so Megan can plan her bakes.</p>
        <p><strong>Pickup.</strong> Orders are handed over at a public meet-up spot around Draper, Utah. There is no shipping.</p>
        <p><strong>Utah Food Freedom Act.</strong> Made in a home kitchen that is not licensed or inspected by the Utah Department of Agriculture and Food or a local health department.</p>
      </div>
      <div class="cta-row" style="margin-top: var(--space-4)">
        <a class="button button--primary" href="/order/">Start an order</a>
        <a class="button button--secondary" href="https://ig.me/m/hearth.honey.bakery">Message on Instagram</a>
      </div>
    </div>
  </section>

</main>

<footer class="site-footer">
  <div class="wrap site-footer__inner">
    <div class="site-footer__brand">
      <img src="/assets/img/logo.svg" alt="Hearth &amp; Honey" width="200" height="80" loading="lazy">
      <p>Small-batch cinnamon rolls, baked at home in Draper, Utah, and handed over in person.</p>
    </div>
    <div>
      <h2>Order or say hi</h2>
      <ul>
        <li><a href="mailto:orders@hearthhoneybakery.com">orders@hearthhoneybakery.com</a></li>
        <li><a href="https://www.instagram.com/hearth.honey.bakery/">@hearth.honey.bakery</a></li>
        <li>Draper, Utah · meet-up pickup</li>
      </ul>
    </div>
    <div>
      <h2>Pages</h2>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/menu/">Menu</a></li>
        <li><a href="/about/">Meet Megan</a></li>
        <li><a href="/order/">Order</a></li>
      </ul>
    </div>
    <div class="site-footer__small">
      <p>Made in a home kitchen that is not licensed or inspected by the Utah Department of Agriculture and Food or a local health department.</p>
      <p>© 2026 Hearth &amp; Honey Bakery · Made with butter, cinnamon, and a lot of care. · <a href="/sitemap.xml">Sitemap</a></p>
    </div>
  </div>
</footer>
</body>
</html>
```

Note: on this page the product card heading is an `h2` (the page's h1 is "What's baking" and the cards are its direct sections). On the home page the same card uses `h3` under the "What's baking" h2. Add to `site.css` in section 8: `.card__body h2 { margin: 0; font-size: var(--step-2); color: var(--red); }` and `.card--placeholder h2 { margin: 0; font-size: var(--step-1); color: var(--red-ink); }`. The allergen note about nuts must be confirmed with Megan; it is the safer default for a home kitchen.

- [ ] **Step 2: Screenshot and check**

Same Playwright one-liner as Task 9 with the URL `http://127.0.0.1:8080/menu/` and output names `menu-390.png`, `menu-1280.png`. Expected: three cards, notes box, CTA row.

- [ ] **Step 3: Commit**

```bash
git add menu/index.html assets/css/site.css
git commit -m "feat: menu page"
```

---

### Task 11: About page

**Files:**
- Create: `about/index.html`

- [ ] **Step 1: Write the page**

```html path=about/index.html
<!DOCTYPE html>
<html lang="en-US" class="no-js">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<script>document.documentElement.className = document.documentElement.className.replace("no-js", "js");</script>
<title>Meet Megan Jackman, Owner and Baker | Hearth &amp; Honey Bakery</title>
<meta name="description" content="Megan Jackman is the baker and owner behind Hearth &amp; Honey, a small-batch home bakery in Draper, Utah. Every order is baked by her, for you.">
<link rel="canonical" href="https://hearthhoneybakery.com/about/">
<meta name="robots" content="index, follow, max-image-preview:large">
<meta name="theme-color" content="#C84C5A">
<meta name="author" content="Megan Jackman">
<meta name="geo.region" content="US-UT">
<meta name="geo.placename" content="Draper, Utah">
<meta property="og:type" content="profile">
<meta property="og:site_name" content="Hearth &amp; Honey Bakery">
<meta property="og:locale" content="en_US">
<meta property="og:title" content="Meet Megan Jackman, Owner and Baker | Hearth &amp; Honey Bakery">
<meta property="og:description" content="Megan Jackman is the baker and owner behind Hearth &amp; Honey, a small-batch home bakery in Draper, Utah.">
<meta property="og:url" content="https://hearthhoneybakery.com/about/">
<meta property="og:image" content="https://hearthhoneybakery.com/assets/img/megan-square-600.jpg">
<meta property="og:image:width" content="600">
<meta property="og:image:height" content="600">
<meta property="og:image:alt" content="Megan Jackman smiling outdoors">
<meta name="twitter:card" content="summary">
<meta name="twitter:title" content="Meet Megan Jackman, Owner and Baker | Hearth &amp; Honey Bakery">
<meta name="twitter:description" content="Megan Jackman is the baker and owner behind Hearth &amp; Honey, a small-batch home bakery in Draper, Utah.">
<meta name="twitter:image" content="https://hearthhoneybakery.com/assets/img/megan-square-600.jpg">
<link rel="icon" href="/favicon.ico" sizes="32x32">
<link rel="icon" href="/assets/img/favicon.svg" type="image/svg+xml">
<link rel="apple-touch-icon" href="/assets/img/apple-touch-icon.png">
<link rel="manifest" href="/site.webmanifest">
<link rel="preload" href="/assets/fonts/fraunces-latin.woff2" as="font" type="font/woff2" crossorigin>
<link rel="preload" href="/assets/fonts/figtree-latin.woff2" as="font" type="font/woff2" crossorigin>
<link rel="stylesheet" href="/assets/css/site.css">
<script type="module" src="/assets/js/site.js"></script>
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "Bakery",
      "@id": "https://hearthhoneybakery.com/#bakery",
      "name": "Hearth & Honey Bakery",
      "alternateName": ["Hearth & Honey", "Hearth and Honey"],
      "description": "Small-batch home bakery in Draper, Utah, known for hand-rolled cinnamon rolls. Order by email or Instagram DM; orders are handed off at a public meet-up spot around Draper.",
      "url": "https://hearthhoneybakery.com/",
      "email": "orders@hearthhoneybakery.com",
      "logo": { "@type": "ImageObject", "url": "https://hearthhoneybakery.com/assets/img/logo-600.png", "width": 600, "height": 240 },
      "image": ["https://hearthhoneybakery.com/assets/img/og-image.png", "https://hearthhoneybakery.com/assets/img/cinnamon-roll-1200.png"],
      "address": { "@type": "PostalAddress", "addressLocality": "Draper", "addressRegion": "UT", "postalCode": "84020", "addressCountry": "US" },
      "areaServed": { "@type": "City", "name": "Draper", "sameAs": "https://en.wikipedia.org/wiki/Draper,_Utah" },
      "sameAs": ["https://www.instagram.com/hearth.honey.bakery/"],
      "founder": { "@id": "https://hearthhoneybakery.com/#megan" },
      "servesCuisine": "Baked goods",
      "hasMenu": { "@id": "https://hearthhoneybakery.com/menu/#menu" },
      "potentialAction": { "@type": "OrderAction", "target": "https://hearthhoneybakery.com/order/", "deliveryMethod": "http://purl.org/goodrelations/v1#DeliveryModePickUp" }
    },
    {
      "@type": "Person",
      "@id": "https://hearthhoneybakery.com/#megan",
      "name": "Megan Jackman",
      "givenName": "Megan",
      "familyName": "Jackman",
      "jobTitle": "Owner and baker",
      "description": "Baker and owner of Hearth & Honey, a small-batch home bakery in Draper, Utah.",
      "worksFor": { "@id": "https://hearthhoneybakery.com/#bakery" },
      "image": "https://hearthhoneybakery.com/assets/img/megan-portrait-800.jpg",
      "url": "https://hearthhoneybakery.com/about/",
      "sameAs": ["https://www.instagram.com/hearth.honey.bakery/"]
    },
    {
      "@type": "WebSite",
      "@id": "https://hearthhoneybakery.com/#website",
      "url": "https://hearthhoneybakery.com/",
      "name": "Hearth & Honey Bakery",
      "publisher": { "@id": "https://hearthhoneybakery.com/#bakery" },
      "inLanguage": "en-US"
    },
    {
      "@type": "WebPage",
      "@id": "https://hearthhoneybakery.com/about/#webpage",
      "url": "https://hearthhoneybakery.com/about/",
      "name": "Meet Megan Jackman, Owner and Baker | Hearth & Honey Bakery",
      "description": "Megan Jackman is the baker and owner behind Hearth & Honey, a small-batch home bakery in Draper, Utah. Every order is baked by her, for you.",
      "isPartOf": { "@id": "https://hearthhoneybakery.com/#website" },
      "about": { "@id": "https://hearthhoneybakery.com/#megan" },
      "primaryImageOfPage": "https://hearthhoneybakery.com/assets/img/megan-portrait-800.jpg",
      "inLanguage": "en-US",
      "datePublished": "2026-09-06",
      "dateModified": "2026-09-06"
    },
    {
      "@type": "BreadcrumbList",
      "@id": "https://hearthhoneybakery.com/about/#breadcrumb",
      "itemListElement": [
        { "@type": "ListItem", "position": 1, "name": "Home", "item": "https://hearthhoneybakery.com/" },
        { "@type": "ListItem", "position": 2, "name": "Meet Megan", "item": "https://hearthhoneybakery.com/about/" }
      ]
    }
  ]
}
</script>
</head>
<body>
<a class="skip-link" href="#main">Skip to main content</a>

<header class="site-header">
  <div class="wrap site-header__inner">
    <a class="brand" href="/"><img src="/assets/img/logo.svg" alt="Hearth &amp; Honey" width="160" height="64"></a>
    <button class="nav-toggle" type="button" aria-expanded="false" aria-controls="site-nav">
      <svg class="icon-open" viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M4 7h16M4 12h16M4 17h16"/></svg>
      <svg class="icon-close" viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M6 6l12 12M18 6 6 18"/></svg>
      <span class="visually-hidden">Site navigation</span>
    </button>
    <nav id="site-nav" class="site-nav" aria-label="Site">
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/menu/">Menu</a></li>
        <li><a href="/about/" aria-current="page">Meet Megan</a></li>
        <li><a class="button button--primary" href="/order/">Order</a></li>
      </ul>
    </nav>
  </div>
</header>

<main id="main" tabindex="-1">

  <section class="section section--tight" aria-labelledby="about-title">
    <div class="wrap about">
      <div class="about__photo">
        <picture>
          <source type="image/webp" srcset="/assets/img/megan-portrait-480.webp 480w, /assets/img/megan-portrait-800.webp 800w" sizes="(min-width: 48em) 24rem, 90vw">
          <img src="/assets/img/megan-portrait-800.jpg" srcset="/assets/img/megan-portrait-480.jpg 480w, /assets/img/megan-portrait-800.jpg 800w" sizes="(min-width: 48em) 24rem, 90vw" alt="Megan Jackman smiling outdoors in front of green trees and a cloudy sky" width="800" height="1067" fetchpriority="high">
        </picture>
      </div>
      <div>
        <p class="eyebrow">The baker</p>
        <h1 id="about-title">Meet Megan</h1>
        <!-- BIO: expand this section whenever Megan is ready. Keep the first paragraph short; it is what search engines quote. -->
        <p class="lede">Megan Jackman is the baker, owner, and glaze-drizzler behind Hearth &amp; Honey.</p>
        <p>She bakes in small batches from her home kitchen in Draper, Utah, which means every order gets her full attention, from the first proof to the last swirl of glaze. When you order, you're talking to her directly. She'll confirm a date, bake that day, and meet you somewhere around Draper to hand it over.</p>

        <h2>Why "Hearth &amp; Honey"?</h2>
        <p><em>Hearth</em> for the home kitchen everything comes out of. <em>Honey</em> for the sweet stuff, and for that warm, sticky, golden feeling of pulling apart a cinnamon roll while it's still steaming.</p>

        <h2>What to expect</h2>
        <ul>
          <li>Real ingredients: flour, butter, eggs, cream, brown sugar, cinnamon, vanilla.</li>
          <li>Made to order, never from a freezer case.</li>
          <li>A reply from Megan herself, usually within a day.</li>
          <li>A meet-up spot around Draper that works for both of you.</li>
        </ul>

        <div class="cta-row" style="margin-top: var(--space-3)">
          <a class="button button--primary" href="/order/">Start an order</a>
          <a class="button button--secondary" href="https://www.instagram.com/hearth.honey.bakery/">Follow on Instagram</a>
        </div>
      </div>
    </div>
  </section>

</main>

<footer class="site-footer">
  <div class="wrap site-footer__inner">
    <div class="site-footer__brand">
      <img src="/assets/img/logo.svg" alt="Hearth &amp; Honey" width="200" height="80" loading="lazy">
      <p>Small-batch cinnamon rolls, baked at home in Draper, Utah, and handed over in person.</p>
    </div>
    <div>
      <h2>Order or say hi</h2>
      <ul>
        <li><a href="mailto:orders@hearthhoneybakery.com">orders@hearthhoneybakery.com</a></li>
        <li><a href="https://www.instagram.com/hearth.honey.bakery/">@hearth.honey.bakery</a></li>
        <li>Draper, Utah · meet-up pickup</li>
      </ul>
    </div>
    <div>
      <h2>Pages</h2>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/menu/">Menu</a></li>
        <li><a href="/about/">Meet Megan</a></li>
        <li><a href="/order/">Order</a></li>
      </ul>
    </div>
    <div class="site-footer__small">
      <p>Made in a home kitchen that is not licensed or inspected by the Utah Department of Agriculture and Food or a local health department.</p>
      <p>© 2026 Hearth &amp; Honey Bakery · Made with butter, cinnamon, and a lot of care. · <a href="/sitemap.xml">Sitemap</a></p>
    </div>
  </div>
</footer>
</body>
</html>
```

Note: the "usually within a day" reply expectation and "never from a freezer case" are claims for Megan to confirm; both are listed in the README's "confirm with Megan" section (Task 15).

- [ ] **Step 2: Screenshot and check** at `/about/` (both widths). Expected: rotated portrait card left, text right on desktop; stacked on mobile.

- [ ] **Step 3: Commit**

```bash
git add about/index.html
git commit -m "feat: about page"
```

---
### Task 12: Order page

**Files:**
- Create: `order/index.html`

**Interfaces:**
- Consumes: `order.js` contract from Task 8 (ids `order-form`, `<field>`, `<field>-error`, `order-errors`, `order-status`, `order-copy`, `order-copy-text`, `order-copy-button`), `ITEMS` slugs from Task 6 (`cinnamon-rolls`, `something-else`).

- [ ] **Step 1: Write the page**

```html path=order/index.html
<!DOCTYPE html>
<html lang="en-US" class="no-js">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<script>document.documentElement.className = document.documentElement.className.replace("no-js", "js");</script>
<title>Order Cinnamon Rolls in Draper, Utah | Hearth &amp; Honey Bakery</title>
<meta name="description" content="Order hand-rolled cinnamon rolls from Hearth &amp; Honey in Draper, Utah. Fill in a quick form to email your order, or message us on Instagram. Meet-up pickup around Draper.">
<link rel="canonical" href="https://hearthhoneybakery.com/order/">
<meta name="robots" content="index, follow, max-image-preview:large">
<meta name="theme-color" content="#C84C5A">
<meta name="author" content="Megan Jackman">
<meta name="geo.region" content="US-UT">
<meta name="geo.placename" content="Draper, Utah">
<meta property="og:type" content="website">
<meta property="og:site_name" content="Hearth &amp; Honey Bakery">
<meta property="og:locale" content="en_US">
<meta property="og:title" content="Order Cinnamon Rolls in Draper, Utah | Hearth &amp; Honey Bakery">
<meta property="og:description" content="Email your order in one tap or message us on Instagram. Meet-up pickup around Draper, Utah.">
<meta property="og:url" content="https://hearthhoneybakery.com/order/">
<meta property="og:image" content="https://hearthhoneybakery.com/assets/img/og-image.png">
<meta property="og:image:width" content="1200">
<meta property="og:image:height" content="630">
<meta property="og:image:alt" content="Hearth &amp; Honey wordmark beside an illustrated cinnamon roll">
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="Order Cinnamon Rolls in Draper, Utah | Hearth &amp; Honey Bakery">
<meta name="twitter:description" content="Email your order in one tap or message us on Instagram. Meet-up pickup around Draper, Utah.">
<meta name="twitter:image" content="https://hearthhoneybakery.com/assets/img/og-image.png">
<link rel="icon" href="/favicon.ico" sizes="32x32">
<link rel="icon" href="/assets/img/favicon.svg" type="image/svg+xml">
<link rel="apple-touch-icon" href="/assets/img/apple-touch-icon.png">
<link rel="manifest" href="/site.webmanifest">
<link rel="preload" href="/assets/fonts/fraunces-latin.woff2" as="font" type="font/woff2" crossorigin>
<link rel="preload" href="/assets/fonts/figtree-latin.woff2" as="font" type="font/woff2" crossorigin>
<link rel="stylesheet" href="/assets/css/site.css">
<script type="module" src="/assets/js/site.js"></script>
<script type="module" src="/assets/js/order.js"></script>
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "Bakery",
      "@id": "https://hearthhoneybakery.com/#bakery",
      "name": "Hearth & Honey Bakery",
      "alternateName": ["Hearth & Honey", "Hearth and Honey"],
      "description": "Small-batch home bakery in Draper, Utah, known for hand-rolled cinnamon rolls. Order by email or Instagram DM; orders are handed off at a public meet-up spot around Draper.",
      "url": "https://hearthhoneybakery.com/",
      "email": "orders@hearthhoneybakery.com",
      "logo": { "@type": "ImageObject", "url": "https://hearthhoneybakery.com/assets/img/logo-600.png", "width": 600, "height": 240 },
      "image": ["https://hearthhoneybakery.com/assets/img/og-image.png", "https://hearthhoneybakery.com/assets/img/cinnamon-roll-1200.png"],
      "address": { "@type": "PostalAddress", "addressLocality": "Draper", "addressRegion": "UT", "postalCode": "84020", "addressCountry": "US" },
      "areaServed": { "@type": "City", "name": "Draper", "sameAs": "https://en.wikipedia.org/wiki/Draper,_Utah" },
      "sameAs": ["https://www.instagram.com/hearth.honey.bakery/"],
      "founder": { "@id": "https://hearthhoneybakery.com/#megan" },
      "servesCuisine": "Baked goods",
      "hasMenu": { "@id": "https://hearthhoneybakery.com/menu/#menu" },
      "potentialAction": { "@type": "OrderAction", "target": "https://hearthhoneybakery.com/order/", "deliveryMethod": "http://purl.org/goodrelations/v1#DeliveryModePickUp" }
    },
    {
      "@type": "Person",
      "@id": "https://hearthhoneybakery.com/#megan",
      "name": "Megan Jackman",
      "jobTitle": "Owner and baker",
      "worksFor": { "@id": "https://hearthhoneybakery.com/#bakery" },
      "image": "https://hearthhoneybakery.com/assets/img/megan-portrait-800.jpg",
      "url": "https://hearthhoneybakery.com/about/"
    },
    {
      "@type": "WebSite",
      "@id": "https://hearthhoneybakery.com/#website",
      "url": "https://hearthhoneybakery.com/",
      "name": "Hearth & Honey Bakery",
      "publisher": { "@id": "https://hearthhoneybakery.com/#bakery" },
      "inLanguage": "en-US"
    },
    {
      "@type": "WebPage",
      "@id": "https://hearthhoneybakery.com/order/#webpage",
      "url": "https://hearthhoneybakery.com/order/",
      "name": "Order Cinnamon Rolls in Draper, Utah | Hearth & Honey Bakery",
      "description": "Order hand-rolled cinnamon rolls from Hearth & Honey in Draper, Utah. Fill in a quick form to email your order, or message us on Instagram. Meet-up pickup around Draper.",
      "isPartOf": { "@id": "https://hearthhoneybakery.com/#website" },
      "about": { "@id": "https://hearthhoneybakery.com/#bakery" },
      "primaryImageOfPage": "https://hearthhoneybakery.com/assets/img/og-image.png",
      "inLanguage": "en-US",
      "datePublished": "2026-09-06",
      "dateModified": "2026-09-06"
    },
    {
      "@type": "BreadcrumbList",
      "@id": "https://hearthhoneybakery.com/order/#breadcrumb",
      "itemListElement": [
        { "@type": "ListItem", "position": 1, "name": "Home", "item": "https://hearthhoneybakery.com/" },
        { "@type": "ListItem", "position": 2, "name": "Order", "item": "https://hearthhoneybakery.com/order/" }
      ]
    },
    {
      "@type": "FAQPage",
      "@id": "https://hearthhoneybakery.com/order/#faq",
      "mainEntity": [
        { "@type": "Question", "name": "How far ahead should I order?", "acceptedAnswer": { "@type": "Answer", "text": "Please give at least 2 days' notice. Megan bakes to order and confirms the exact date when she replies." } },
        { "@type": "Question", "name": "Where do we meet?", "acceptedAnswer": { "@type": "Answer", "text": "At a public spot around Draper, Utah, agreed when Megan confirms your order." } },
        { "@type": "Question", "name": "Do you deliver or ship?", "acceptedAnswer": { "@type": "Answer", "text": "Meet-up handoff only. There is no shipping." } },
        { "@type": "Question", "name": "What about allergens?", "acceptedAnswer": { "@type": "Answer", "text": "Cinnamon rolls contain wheat, milk, and eggs. Everything is made in a home kitchen that is not allergen-free." } },
        { "@type": "Question", "name": "How do I pay?", "acceptedAnswer": { "@type": "Answer", "text": "Megan confirms payment details along with your order date." } }
      ]
    }
  ]
}
</script>
</head>
<body>
<a class="skip-link" href="#main">Skip to main content</a>

<header class="site-header">
  <div class="wrap site-header__inner">
    <a class="brand" href="/"><img src="/assets/img/logo.svg" alt="Hearth &amp; Honey" width="160" height="64"></a>
    <button class="nav-toggle" type="button" aria-expanded="false" aria-controls="site-nav">
      <svg class="icon-open" viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M4 7h16M4 12h16M4 17h16"/></svg>
      <svg class="icon-close" viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M6 6l12 12M18 6 6 18"/></svg>
      <span class="visually-hidden">Site navigation</span>
    </button>
    <nav id="site-nav" class="site-nav" aria-label="Site">
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/menu/">Menu</a></li>
        <li><a href="/about/">Meet Megan</a></li>
        <li><a class="button button--primary" href="/order/" aria-current="page">Order</a></li>
      </ul>
    </nav>
  </div>
</header>

<main id="main" tabindex="-1">

  <div class="wrap wrap--narrow page-hero">
    <p class="eyebrow">Order</p>
    <h1>Order from Hearth &amp; Honey</h1>
    <p class="lede">Two ways to order, both easy. Fill out the form and it opens an email to us with everything filled in, or skip it and message us on Instagram. Either way, Megan replies personally to confirm a date, a meet-up spot, and payment.</p>
  </div>

  <section class="section section--tight" aria-labelledby="form-title">
    <div class="wrap wrap--narrow">
      <form id="order-form" class="form order-form" action="mailto:orders@hearthhoneybakery.com" method="post" enctype="text/plain" novalidate>
        <fieldset>
          <legend id="form-title">Your order</legend>
          <div class="form">
            <div id="order-errors" class="error-summary" tabindex="-1" hidden></div>

            <div class="form__row">
              <div class="field">
                <label for="name">Your name</label>
                <input id="name" name="name" type="text" autocomplete="name" required aria-required="true" aria-describedby="name-error">
                <p id="name-error" class="error" aria-live="polite"></p>
              </div>
              <div class="field">
                <label for="email">Your email</label>
                <p id="email-hint" class="hint">So Megan can reply.</p>
                <input id="email" name="email" type="email" autocomplete="email" inputmode="email" required aria-required="true" aria-describedby="email-hint email-error">
                <p id="email-error" class="error" aria-live="polite"></p>
              </div>
            </div>

            <div class="form__row">
              <div class="field">
                <label for="item">What would you like?</label>
                <select id="item" name="item" required aria-required="true" aria-describedby="item-error">
                  <option value="cinnamon-rolls">Cinnamon Rolls</option>
                  <option value="something-else">Something else (tell us in the notes)</option>
                </select>
                <p id="item-error" class="error" aria-live="polite"></p>
              </div>
              <div class="field">
                <label for="quantity">How many?</label>
                <p id="quantity-hint" class="hint">Not sure? Put a rough number and Megan will help.</p>
                <input id="quantity" name="quantity" type="number" inputmode="numeric" min="1" step="1" required aria-required="true" aria-describedby="quantity-hint quantity-error">
                <p id="quantity-error" class="error" aria-live="polite"></p>
              </div>
            </div>

            <div class="form__row">
              <div class="field">
                <label for="date">When would you like them?</label>
                <p id="date-hint" class="hint">Please give at least 2 days' notice.</p>
                <input id="date" name="date" type="date" required aria-required="true" aria-describedby="date-hint date-error">
                <p id="date-error" class="error" aria-live="polite"></p>
              </div>
              <div class="field">
                <label for="instagram">Instagram handle <span class="hint">(optional)</span></label>
                <p id="instagram-hint" class="hint">If you'd rather we reply by DM.</p>
                <input id="instagram" name="instagram" type="text" autocomplete="off" aria-describedby="instagram-hint instagram-error">
                <p id="instagram-error" class="error" aria-live="polite"></p>
              </div>
            </div>

            <div class="field">
              <label for="notes">Anything else? <span class="hint">(optional)</span></label>
              <p id="notes-hint" class="hint">Occasion, allergies, meet-up preferences, or the thing you have in mind.</p>
              <textarea id="notes" name="notes" rows="4" aria-describedby="notes-hint notes-error"></textarea>
              <p id="notes-error" class="error" aria-live="polite"></p>
            </div>

            <div class="cta-row">
              <button class="button button--primary" type="submit">Open my order email</button>
              <p class="hint" style="margin:0">This opens your mail app with the order filled in. Nothing is sent until you press send.</p>
            </div>
            <p id="order-status" class="status" role="status" aria-live="polite"></p>

            <div id="order-copy" class="copy-panel" hidden>
              <p><strong>Email didn't open?</strong> Copy the details below and send them to <a href="mailto:orders@hearthhoneybakery.com">orders@hearthhoneybakery.com</a>.</p>
              <label for="order-copy-text" class="visually-hidden">Your order details</label>
              <textarea id="order-copy-text" readonly></textarea>
              <div><button id="order-copy-button" class="button button--secondary" type="button">Copy order details</button></div>
            </div>
          </div>
        </fieldset>
      </form>

      <div class="order-fallback notes">
        <p><strong>Email us your order.</strong> Send what you'd like, how many, the date, and your name to <a href="mailto:orders@hearthhoneybakery.com?subject=Order%20request">orders@hearthhoneybakery.com</a>.</p>
        <p>Or <a href="https://ig.me/m/hearth.honey.bakery">message us on Instagram</a>.</p>
      </div>
    </div>
  </section>

  <section class="band" aria-labelledby="dm-title">
    <div class="wrap band__inner">
      <div>
        <p class="eyebrow">Prefer Instagram?</p>
        <h2 id="dm-title">Send us a DM instead.</h2>
        <p>Tell us what you'd like, how many, and when. Megan reads every message.</p>
      </div>
      <a class="button button--light" href="https://ig.me/m/hearth.honey.bakery">
        <svg viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><rect x="3" y="3" width="18" height="18" rx="5"/><circle cx="12" cy="12" r="4"/><circle cx="17.5" cy="6.5" r="1" fill="currentColor" stroke="none"/></svg>
        Message @hearth.honey.bakery
      </a>
    </div>
    <svg class="drip" viewBox="0 0 1440 60" preserveAspectRatio="none" aria-hidden="true"><path fill="currentColor" d="M0 0H1440V22C1400 22 1392 50 1362 50C1332 50 1326 22 1290 22H1080C1050 22 1044 58 1010 58C976 58 970 22 930 22H730C700 22 694 42 668 42C642 42 636 22 596 22H380C350 22 344 54 310 54C276 54 270 22 230 22H0Z"/></svg>
  </section>

  <section class="section" aria-labelledby="faq-title">
    <div class="wrap wrap--narrow">
      <div class="section__head">
        <p class="eyebrow">Good to know</p>
        <h2 id="faq-title">Questions people ask</h2>
      </div>
      <div class="faq">
        <div>
          <h3>How far ahead should I order?</h3>
          <p>Please give at least 2 days' notice. Megan bakes to order and confirms the exact date when she replies.</p>
        </div>
        <div>
          <h3>Where do we meet?</h3>
          <p>At a public spot around Draper, Utah, agreed when Megan confirms your order.</p>
        </div>
        <div>
          <h3>Do you deliver or ship?</h3>
          <p>Meet-up handoff only. There is no shipping.</p>
        </div>
        <div>
          <h3>What about allergens?</h3>
          <p>Cinnamon rolls contain wheat, milk, and eggs. Everything is made in a home kitchen that is not allergen-free.</p>
        </div>
        <div>
          <h3>How do I pay?</h3>
          <p>Megan confirms payment details along with your order date.</p>
        </div>
      </div>
    </div>
  </section>

</main>

<footer class="site-footer">
  <div class="wrap site-footer__inner">
    <div class="site-footer__brand">
      <img src="/assets/img/logo.svg" alt="Hearth &amp; Honey" width="200" height="80" loading="lazy">
      <p>Small-batch cinnamon rolls, baked at home in Draper, Utah, and handed over in person.</p>
    </div>
    <div>
      <h2>Order or say hi</h2>
      <ul>
        <li><a href="mailto:orders@hearthhoneybakery.com">orders@hearthhoneybakery.com</a></li>
        <li><a href="https://www.instagram.com/hearth.honey.bakery/">@hearth.honey.bakery</a></li>
        <li>Draper, Utah · meet-up pickup</li>
      </ul>
    </div>
    <div>
      <h2>Pages</h2>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/menu/">Menu</a></li>
        <li><a href="/about/">Meet Megan</a></li>
        <li><a href="/order/">Order</a></li>
      </ul>
    </div>
    <div class="site-footer__small">
      <p>Made in a home kitchen that is not licensed or inspected by the Utah Department of Agriculture and Food or a local health department.</p>
      <p>© 2026 Hearth &amp; Honey Bakery · Made with butter, cinnamon, and a lot of care. · <a href="/sitemap.xml">Sitemap</a></p>
    </div>
  </div>
</footer>
</body>
</html>
```

Notes: the footer band on this page sits above the FAQ section, so the FAQ section needs top clearance for the drip. Add to `site.css` section 11: `.band + .section { padding-top: calc(var(--space-6) + 1.5rem); }`. The two inline `style` attributes on this and earlier pages are the only ones on the site; html-validate's `no-inline-style` rule is switched off in Task 13's config.

- [ ] **Step 2: Screenshot and exercise** at `/order/?item=cinnamon-rolls`. Expected: form with two-column rows on desktop, single column on mobile; submitting empty shows the red error summary and per-field messages.

- [ ] **Step 3: Commit**

```bash
git add order/index.html assets/css/site.css
git commit -m "feat: order page with mail composer and FAQ"
```

---

### Task 13: 404 and crawler/platform files

**Files:**
- Create: `404.html`, `robots.txt`, `sitemap.xml`, `llms.txt`, `site.webmanifest`, `CNAME`, `.nojekyll`, `tools/.htmlvalidate.json`

- [ ] **Step 1: Write `404.html`** (GitHub Pages serves this for unknown paths; all links are root-absolute)

```html path=404.html
<!DOCTYPE html>
<html lang="en-US" class="no-js">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<script>document.documentElement.className = document.documentElement.className.replace("no-js", "js");</script>
<title>Page not found | Hearth &amp; Honey Bakery</title>
<meta name="robots" content="noindex">
<meta name="theme-color" content="#C84C5A">
<link rel="icon" href="/favicon.ico" sizes="32x32">
<link rel="icon" href="/assets/img/favicon.svg" type="image/svg+xml">
<link rel="apple-touch-icon" href="/assets/img/apple-touch-icon.png">
<link rel="stylesheet" href="/assets/css/site.css">
<script type="module" src="/assets/js/site.js"></script>
</head>
<body>
<a class="skip-link" href="#main">Skip to main content</a>

<header class="site-header">
  <div class="wrap site-header__inner">
    <a class="brand" href="/"><img src="/assets/img/logo.svg" alt="Hearth &amp; Honey" width="160" height="64"></a>
    <button class="nav-toggle" type="button" aria-expanded="false" aria-controls="site-nav">
      <svg class="icon-open" viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M4 7h16M4 12h16M4 17h16"/></svg>
      <svg class="icon-close" viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M6 6l12 12M18 6 6 18"/></svg>
      <span class="visually-hidden">Site navigation</span>
    </button>
    <nav id="site-nav" class="site-nav" aria-label="Site">
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/menu/">Menu</a></li>
        <li><a href="/about/">Meet Megan</a></li>
        <li><a class="button button--primary" href="/order/">Order</a></li>
      </ul>
    </nav>
  </div>
</header>

<main id="main" tabindex="-1">
  <div class="wrap wrap--narrow not-found">
    <img src="/assets/img/cinnamon-roll.svg" alt="" width="320" height="250">
    <p class="eyebrow">404</p>
    <h1>This page got eaten.</h1>
    <p class="lede">Whatever was here is gone, and it was probably delicious. Try one of these instead.</p>
    <div class="cta-row">
      <a class="button button--primary" href="/">Back home</a>
      <a class="button button--secondary" href="/menu/">See the menu</a>
      <a class="button button--secondary" href="/order/">Place an order</a>
    </div>
  </div>
</main>

<footer class="site-footer">
  <div class="wrap site-footer__inner">
    <div class="site-footer__brand">
      <img src="/assets/img/logo.svg" alt="Hearth &amp; Honey" width="200" height="80" loading="lazy">
      <p>Small-batch cinnamon rolls, baked at home in Draper, Utah, and handed over in person.</p>
    </div>
    <div>
      <h2>Order or say hi</h2>
      <ul>
        <li><a href="mailto:orders@hearthhoneybakery.com">orders@hearthhoneybakery.com</a></li>
        <li><a href="https://www.instagram.com/hearth.honey.bakery/">@hearth.honey.bakery</a></li>
        <li>Draper, Utah · meet-up pickup</li>
      </ul>
    </div>
    <div>
      <h2>Pages</h2>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/menu/">Menu</a></li>
        <li><a href="/about/">Meet Megan</a></li>
        <li><a href="/order/">Order</a></li>
      </ul>
    </div>
    <div class="site-footer__small">
      <p>Made in a home kitchen that is not licensed or inspected by the Utah Department of Agriculture and Food or a local health department.</p>
      <p>© 2026 Hearth &amp; Honey Bakery · Made with butter, cinnamon, and a lot of care. · <a href="/sitemap.xml">Sitemap</a></p>
    </div>
  </div>
</footer>
</body>
</html>
```

- [ ] **Step 2: Write the crawler files**

```text path=robots.txt
# Hearth & Honey Bakery — everyone is welcome, including AI crawlers.
User-agent: *
Allow: /

User-agent: GPTBot
Allow: /

User-agent: ChatGPT-User
Allow: /

User-agent: ClaudeBot
Allow: /

User-agent: Claude-User
Allow: /

User-agent: PerplexityBot
Allow: /

User-agent: Google-Extended
Allow: /

User-agent: Applebot-Extended
Allow: /

Sitemap: https://hearthhoneybakery.com/sitemap.xml
```

```xml path=sitemap.xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:image="http://www.google.com/schemas/sitemap-image/1.1">
  <url>
    <loc>https://hearthhoneybakery.com/</loc>
    <lastmod>2026-09-06</lastmod>
    <image:image>
      <image:loc>https://hearthhoneybakery.com/assets/img/cinnamon-roll-1200.png</image:loc>
      <image:title>Hearth &amp; Honey cinnamon roll</image:title>
    </image:image>
  </url>
  <url>
    <loc>https://hearthhoneybakery.com/menu/</loc>
    <lastmod>2026-09-06</lastmod>
    <image:image>
      <image:loc>https://hearthhoneybakery.com/assets/img/cinnamon-roll-1200.png</image:loc>
      <image:title>Cinnamon Rolls</image:title>
    </image:image>
  </url>
  <url>
    <loc>https://hearthhoneybakery.com/about/</loc>
    <lastmod>2026-09-06</lastmod>
    <image:image>
      <image:loc>https://hearthhoneybakery.com/assets/img/megan-portrait-800.jpg</image:loc>
      <image:title>Megan Jackman, owner and baker</image:title>
    </image:image>
  </url>
  <url>
    <loc>https://hearthhoneybakery.com/order/</loc>
    <lastmod>2026-09-06</lastmod>
  </url>
</urlset>
```

```text path=llms.txt
# Hearth & Honey Bakery

> Small-batch home bakery in Draper, Utah, run by owner and baker Megan Jackman. Known for hand-rolled cinnamon rolls made to order. Orders are placed by email (orders@hearthhoneybakery.com) or Instagram DM (@hearth.honey.bakery) and handed over at a public meet-up spot around Draper. No shipping, no online checkout, no prices published online.

Key facts:
- Business name: Hearth & Honey Bakery (also written "Hearth and Honey")
- Owner and baker: Megan Jackman
- Location: Draper, Utah 84020, United States (home bakery; city only)
- Order by email: orders@hearthhoneybakery.com
- Order by Instagram: https://www.instagram.com/hearth.honey.bakery/
- Fulfillment: meet-up handoff at a public place around Draper; at least 2 days' notice
- Legal: made in a home kitchen that is not licensed or inspected by the Utah Department of Agriculture and Food or a local health department (Utah Food Freedom Act)

## Menu

- [Cinnamon Rolls](https://hearthhoneybakery.com/menu/#cinnamon-rolls): soft dough rolled with brown sugar and cinnamon, baked golden, finished with a vanilla-cream glaze. Ingredients: flour, milk, brown sugar, heavy cream, butter, eggs, water, sugar, yeast, salt, vanilla, cinnamon. Contains wheat, milk, eggs.

## Pages

- [Home](https://hearthhoneybakery.com/): overview and how ordering works
- [Menu](https://hearthhoneybakery.com/menu/): everything you can order, with ingredients and allergens
- [Meet Megan](https://hearthhoneybakery.com/about/): about the owner
- [Order](https://hearthhoneybakery.com/order/): order form that composes an email, Instagram DM link, FAQ

## Optional

- [Sitemap](https://hearthhoneybakery.com/sitemap.xml)
```

```json path=site.webmanifest
{
  "name": "Hearth & Honey Bakery",
  "short_name": "Hearth & Honey",
  "description": "Hand-rolled cinnamon rolls from a small-batch home bakery in Draper, Utah.",
  "start_url": "/",
  "display": "browser",
  "background_color": "#FAF3E8",
  "theme_color": "#C84C5A",
  "icons": [
    { "src": "/assets/img/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/assets/img/icon-512.png", "sizes": "512x512", "type": "image/png" }
  ]
}
```

```text path=CNAME
hearthhoneybakery.com
```

Create the empty `.nojekyll` with `touch .nojekyll` (it tells GitHub Pages to serve files as-is).

- [ ] **Step 3: Write the html-validate config**

```json path=tools/.htmlvalidate.json
{
  "extends": ["html-validate:recommended"],
  "rules": {
    "no-inline-style": "off",
    "no-trailing-whitespace": "off",
    "long-title": "off",
    "require-sri": "off",
    "svg-focusable": "off",
    "prefer-native-element": "off"
  }
}
```

- [ ] **Step 4: Validate all pages**

Run: `cd tools && npm run verify:html`
Expected: no errors. If a rule fires on intended markup (for example `heading-level` on the footer `h2`s, or `wcag/h63` on a table that does not exist), read the message, fix real problems in the HTML, and only add a rule to the `off` list when the markup is correct by WCAG and the rule is stylistic. Record every rule you turn off and why in the commit message.

- [ ] **Step 5: Commit**

```bash
git add 404.html robots.txt sitemap.xml llms.txt site.webmanifest CNAME .nojekyll tools/.htmlvalidate.json
git commit -m "feat: 404, sitemap, robots, llms.txt, manifest, CNAME"
```

---
### Task 14: Verification harness, run it, fix what it finds

**Files:**
- Create: `tools/serve.mjs`, `tools/verify/links.mjs`, `tools/verify/jsonld.mjs`, `tools/verify/axe.mjs`, `tools/verify/lighthouse.mjs`
- Modify: whatever the checks flag (pages, CSS)

**Interfaces:**
- `startServer(port = 0) → Promise<{ url, close }>` from `tools/serve.mjs`, used by axe and lighthouse scripts.

- [ ] **Step 1: Write the static server**

```js path=tools/serve.mjs
// Tiny static server that mimics GitHub Pages: clean URLs, index.html per folder, 404.html for misses.
// Run alone: npm run serve  (http://127.0.0.1:8080)   Used by the verify scripts on a random port.
import { createServer } from 'node:http';
import { readFile, stat } from 'node:fs/promises';
import { join, extname } from 'node:path';
import { fileURLToPath } from 'node:url';

const ROOT = fileURLToPath(new URL('../', import.meta.url));
const TYPES = {
  '.html': 'text/html; charset=utf-8', '.css': 'text/css; charset=utf-8', '.js': 'text/javascript; charset=utf-8',
  '.svg': 'image/svg+xml', '.png': 'image/png', '.jpg': 'image/jpeg', '.webp': 'image/webp', '.ico': 'image/x-icon',
  '.xml': 'application/xml', '.txt': 'text/plain; charset=utf-8', '.webmanifest': 'application/manifest+json', '.woff2': 'font/woff2',
};
const isFile = async (p) => { try { return (await stat(p)).isFile(); } catch { return false; } };

export function startServer(port = 0) {
  const server = createServer(async (req, res) => {
    const path = decodeURIComponent(new URL(req.url, 'http://local').pathname);
    if (path.includes('..')) { res.writeHead(400); res.end(); return; }
    let file = join(ROOT, path);
    if (path.endsWith('/')) file = join(file, 'index.html');
    else if (!(await isFile(file)) && (await isFile(join(file, 'index.html')))) { res.writeHead(301, { Location: `${path}/` }); res.end(); return; }
    let status = 200;
    if (!(await isFile(file))) { status = 404; file = join(ROOT, '404.html'); }
    res.writeHead(status, { 'Content-Type': TYPES[extname(file)] || 'application/octet-stream', 'Cache-Control': 'no-store' });
    res.end(await readFile(file));
  });
  return new Promise((resolve) => server.listen(port, '127.0.0.1', () => resolve({ url: `http://127.0.0.1:${server.address().port}`, close: () => server.close() })));
}

if (process.argv[1] === fileURLToPath(import.meta.url)) {
  const { url } = await startServer(8080);
  console.log(`Serving ${ROOT} at ${url}`);
}
```

- [ ] **Step 2: Write the link and sitemap checker**

```js path=tools/verify/links.mjs
// Every root-absolute href/src/content URL must exist on disk; sitemap and pages must agree.
import { readFileSync, existsSync, readdirSync, statSync } from 'node:fs';
import { join } from 'node:path';
import { fileURLToPath } from 'node:url';

const ROOT = fileURLToPath(new URL('../../', import.meta.url));
const SITE = 'https://hearthhoneybakery.com';
const SKIP = new Set(['node_modules', 'tools', 'docs', '.git']);

function htmlFiles(dir) {
  return readdirSync(dir).flatMap((name) => {
    if (SKIP.has(name)) return [];
    const p = join(dir, name);
    return statSync(p).isDirectory() ? htmlFiles(p) : name.endsWith('.html') ? [p] : [];
  });
}

let failed = 0;
const pages = htmlFiles(ROOT);
for (const file of pages) {
  const html = readFileSync(file, 'utf8');
  const refs = [...html.matchAll(/(?:href|src|content)="([^"]+)"/g)].map((m) => m[1]);
  for (const raw of refs) {
    if (!(raw.startsWith('/') || raw.startsWith(SITE))) continue;
    const path = raw.replace(SITE, '').split('#')[0].split('?')[0];
    if (!path) continue;
    const target = path.endsWith('/') ? join(ROOT, path, 'index.html') : join(ROOT, path);
    if (!existsSync(target)) { failed++; console.log(`MISSING  ${raw}   (in ${file.slice(ROOT.length)})`); }
  }
}
const sitemap = readFileSync(join(ROOT, 'sitemap.xml'), 'utf8');
for (const [, loc] of sitemap.matchAll(/<loc>([^<]+)<\/loc>/g)) {
  const path = loc.replace(SITE, '');
  const target = path.endsWith('/') ? join(ROOT, path, 'index.html') : join(ROOT, path);
  if (!existsSync(target)) { failed++; console.log(`SITEMAP  points at missing file: ${loc}`); }
}
for (const file of pages) {
  const rel = '/' + file.slice(ROOT.length).replaceAll('\\', '/').replace(/index\.html$/, '');
  if (rel !== '/404.html' && !sitemap.includes(`<loc>${SITE}${rel}</loc>`)) { failed++; console.log(`SITEMAP  page not listed: ${rel}`); }
}
console.log(failed ? `${failed} link problem(s)` : `All links, assets, and sitemap entries resolve (${pages.length} pages)`);
process.exit(failed ? 1 : 0);
```

- [ ] **Step 3: Write the structured-data checker**

```js path=tools/verify/jsonld.mjs
// Every JSON-LD block must parse, carry the expected @types, and have the fields Google/AI readers look for.
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';

const ROOT = fileURLToPath(new URL('../../', import.meta.url));
const REQUIRED = {
  Bakery: ['name', 'url', 'email', 'address', 'sameAs', 'founder', 'image', 'logo'],
  Person: ['name', 'jobTitle', 'image', 'url'],
  WebSite: ['url', 'name', 'publisher'],
  WebPage: ['url', 'name', 'description', 'isPartOf'],
  BreadcrumbList: ['itemListElement'],
  Menu: ['hasMenuSection'],
  Product: ['name', 'description', 'image', 'brand'],
  FAQPage: ['mainEntity'],
};
const PAGES = {
  'index.html': ['Bakery', 'Person', 'WebSite', 'WebPage', 'BreadcrumbList', 'Menu', 'Product'],
  'menu/index.html': ['Bakery', 'Person', 'WebSite', 'WebPage', 'BreadcrumbList', 'Menu', 'Product'],
  'about/index.html': ['Bakery', 'Person', 'WebSite', 'WebPage', 'BreadcrumbList'],
  'order/index.html': ['Bakery', 'Person', 'WebSite', 'WebPage', 'BreadcrumbList', 'FAQPage'],
};

let failed = 0;
for (const [file, expected] of Object.entries(PAGES)) {
  const html = readFileSync(ROOT + file, 'utf8');
  const blocks = [...html.matchAll(/<script type="application\/ld\+json">([\s\S]*?)<\/script>/g)].map((m) => m[1]);
  if (blocks.length === 0) { failed++; console.log(`${file}: no JSON-LD block`); continue; }
  const nodes = [];
  for (const block of blocks) {
    try { const json = JSON.parse(block); nodes.push(...(json['@graph'] || [json])); }
    catch (err) { failed++; console.log(`${file}: JSON-LD parse error: ${err.message}`); }
  }
  const types = nodes.map((n) => n['@type']);
  for (const type of expected) if (!types.includes(type)) { failed++; console.log(`${file}: missing @type ${type}`); }
  for (const node of nodes) for (const key of REQUIRED[node['@type']] || []) {
    if (node[key] === undefined) { failed++; console.log(`${file}: ${node['@type']} is missing "${key}"`); }
  }
  const ids = new Set(nodes.map((n) => n['@id']).filter(Boolean));
  for (const ref of JSON.stringify(nodes).matchAll(/\{"@id":"([^"]+)"\}/g)) {
    if (!ids.has(ref[1])) { failed++; console.log(`${file}: reference to undefined @id ${ref[1]}`); }
  }
  const canonical = /<link rel="canonical" href="([^"]+)"/.exec(html)?.[1];
  const webpage = nodes.find((n) => n['@type'] === 'WebPage');
  if (webpage && webpage.url !== canonical) { failed++; console.log(`${file}: WebPage.url ${webpage.url} != canonical ${canonical}`); }
  console.log(`${file}: ${nodes.length} nodes (${types.join(', ')})`);
}
console.log(failed ? `${failed} structured-data problem(s)` : 'Structured data OK');
process.exit(failed ? 1 : 0);
```

- [ ] **Step 4: Write the axe + behavior checker**

```js path=tools/verify/axe.mjs
// axe-core WCAG 2.2 AA scan of every page at two widths, then keyboard/behavior checks that axe cannot see.
import { chromium } from 'playwright-core';
import AxeBuilder from '@axe-core/playwright';
import { startServer } from '../serve.mjs';

const PAGES = ['/', '/menu/', '/about/', '/order/', '/404.html'];
const TAGS = ['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa', 'wcag22aa', 'best-practice'];
const { url, close } = await startServer();
const browser = await chromium.launch({ channel: 'chrome', headless: true });
let failures = 0;
const check = (ok, message) => { console.log(`${ok ? 'PASS' : 'FAIL'}  ${message}`); if (!ok) failures++; };

for (const viewport of [{ width: 390, height: 844 }, { width: 1280, height: 900 }]) {
  const context = await browser.newContext({ viewport });
  const page = await context.newPage();
  for (const path of PAGES) {
    await page.goto(url + path, { waitUntil: 'networkidle' });
    const { violations } = await new AxeBuilder({ page }).withTags(TAGS).analyze();
    console.log(`${viewport.width}px ${path}: ${violations.length} violation(s)`);
    for (const v of violations) {
      failures++;
      console.log(`  - [${v.impact}] ${v.id}: ${v.help}`);
      for (const node of v.nodes.slice(0, 4)) console.log(`      ${node.target.join(' ')}`);
    }
  }
  await context.close();
}

// Keyboard and behavior, mobile width
const mobile = await browser.newContext({ viewport: { width: 390, height: 844 } });
const page = await mobile.newPage();
await page.goto(url + '/', { waitUntil: 'networkidle' });
await page.keyboard.press('Tab');
check(await page.evaluate(() => document.activeElement?.classList.contains('skip-link')), 'first Tab lands on the skip link');
await page.keyboard.press('Enter');
check(await page.evaluate(() => location.hash === '#main'), 'skip link jumps to #main');
const toggle = page.locator('.nav-toggle');
await toggle.focus();
await page.keyboard.press('Enter');
check((await toggle.getAttribute('aria-expanded')) === 'true', 'nav toggle opens with Enter');
check(await page.locator('#site-nav a').first().isVisible(), 'nav links are visible when open');
await page.keyboard.press('Escape');
check((await toggle.getAttribute('aria-expanded')) === 'false', 'Escape closes the nav');
check(await page.evaluate(() => document.activeElement?.classList.contains('nav-toggle')), 'focus returns to the toggle after Escape');

await page.goto(url + '/order/?item=cinnamon-rolls', { waitUntil: 'networkidle' });
check((await page.locator('#item').inputValue()) === 'cinnamon-rolls', '?item= preselects the item');
check((await page.locator('#date').getAttribute('min')) !== null, 'date field has a min set by script');
await page.locator('#order-form button[type=submit]').click();
check(await page.locator('#order-errors').isVisible(), 'error summary appears on an empty submit');
check(await page.evaluate(() => document.activeElement?.id === 'order-errors'), 'error summary receives focus');
check((await page.locator('#name').getAttribute('aria-invalid')) === 'true', 'invalid field is marked aria-invalid');
check((await page.locator('#name-error').textContent()).length > 0, 'inline error message is shown');
await page.locator('#order-errors a').first().click();
check(await page.evaluate(() => document.activeElement?.id === 'name'), 'error summary link moves focus to the field');
await page.fill('#name', 'Sam Lee');
check((await page.locator('#name').getAttribute('aria-invalid')) === 'false', 'error clears when the field is corrected');
await page.fill('#email', 'sam@example.com');
await page.fill('#quantity', '12');
await page.fill('#date', await page.locator('#date').getAttribute('min'));
await page.locator('#order-form button[type=submit]').click();
check(await page.locator('#order-copy').isVisible(), 'copy panel appears after a valid submit');
const composed = await page.locator('#order-copy-text').inputValue();
check(composed.includes('To: orders@hearthhoneybakery.com'), 'composed email is addressed to orders@');
check(composed.includes('Subject: Order request: Cinnamon Rolls × 12'), 'composed subject carries item and quantity');
check(composed.includes('Name: Sam Lee'), 'composed body carries the name');
await mobile.close();

// Reduced motion
const reduced = await browser.newContext({ viewport: { width: 1280, height: 900 }, reducedMotion: 'reduce' });
const r = await reduced.newPage();
await r.goto(url + '/', { waitUntil: 'networkidle' });
check((await r.evaluate(() => document.getAnimations().filter((a) => a.playState === 'running').length)) === 0, 'no running animations under prefers-reduced-motion');
await reduced.close();
const normal = await browser.newContext({ viewport: { width: 1280, height: 900 } });
const n = await normal.newPage();
await n.goto(url + '/', { waitUntil: 'networkidle' });
check((await n.evaluate(() => document.getAnimations().length)) > 0, 'steam and badge animate when motion is allowed');
await normal.close();

await browser.close();
close();
if (failures) { console.error(`\n${failures} accessibility/behavior failure(s)`); process.exit(1); }
console.log('\nAll accessibility and behavior checks passed');
```

- [ ] **Step 5: Write the Lighthouse runner**

```js path=tools/verify/lighthouse.mjs
// Mobile Lighthouse on every page against the spec targets. HTML reports land in tools/reports/.
import lighthouse from 'lighthouse';
import * as chromeLauncher from 'chrome-launcher';
import { writeFileSync, mkdirSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { startServer } from '../serve.mjs';

const TARGETS = { performance: 95, accessibility: 100, 'best-practices': 95, seo: 100 };
const PAGES = ['/', '/menu/', '/about/', '/order/'];
const REPORTS = fileURLToPath(new URL('../reports/', import.meta.url));
mkdirSync(REPORTS, { recursive: true });

const { url, close } = await startServer();
const chrome = await chromeLauncher.launch({ chromeFlags: ['--headless=new', '--no-sandbox'] });
let failed = 0;
for (const path of PAGES) {
  const { lhr, report } = await lighthouse(url + path, { port: chrome.port, output: 'html', logLevel: 'error', onlyCategories: Object.keys(TARGETS) });
  writeFileSync(`${REPORTS}lighthouse-${path.replaceAll('/', '') || 'home'}.html`, report);
  const cells = Object.entries(TARGETS).map(([cat, min]) => {
    const score = Math.round(lhr.categories[cat].score * 100);
    if (score < min) failed++;
    return `${cat}=${score}${score < min ? ` (target ${min})` : ''}`;
  });
  console.log(`${path.padEnd(8)} ${cells.join('   ')}`);
}
await chrome.kill();
close();
if (failed) { console.error(`${failed} score(s) under target`); process.exit(1); }
console.log('All Lighthouse targets met');
```

- [ ] **Step 6: Run the whole suite**

Run: `cd tools && npm run verify`
Expected: unit tests pass, contrast passes, html-validate clean, links clean, structured data OK, axe zero violations and all behavior checks PASS, Lighthouse meets targets on all four pages.

For each failure: read the message, fix the root cause in the page or stylesheet (never by loosening the check), re-run only that script, then re-run the full suite once at the end. Common ones to expect and how to fix them:
- axe `color-contrast` on `.hint` or `.card__meta` text: those use `--ink-soft`; if flagged on `--cream-2`, darken `--ink-soft` to `#5E4D43` and re-run contrast.
- axe `landmark-unique` if two `<nav>`s share a label: only one nav exists; check the footer lists are not `<nav>`.
- Lighthouse Performance under 95: check the logo SVG size (should be under 40 KB; if larger, raise `optTolerance` to 0.5 in `build-images.mjs`), confirm fonts are preloaded, and confirm the hero image has `fetchpriority="high"`.
- Lighthouse SEO under 100: usually a missing `meta description` or a link without discernible text.

- [ ] **Step 7: Visual review**

Screenshot all five pages at 390 and 1280 as in Task 9 Step 2 and view every image. Look for: overlapping drips, cut-off badge, orphaned headings, cramped cards, the mobile nav panel rendering above the hero. Fix in `site.css` and re-screenshot until it looks finished.

- [ ] **Step 8: Commit**

```bash
git add tools/serve.mjs tools/verify assets index.html menu about order 404.html
git commit -m "test: verification harness (links, JSON-LD, axe, Lighthouse) and fixes"
```

---

### Task 15: README and handoff notes

**Files:**
- Create: `README.md`

- [ ] **Step 1: Write the README**

```markdown path=README.md
# Hearth & Honey — website

The public site for Hearth & Honey Bakery, Draper, Utah. Live at https://hearthhoneybakery.com, hosted on GitHub Pages from the root of the `main` branch.

Plain HTML, one CSS file, two small scripts. There is no build step: edit a file, commit, push, and GitHub Pages publishes it within a minute or two.

## Where things are

| Path | What it is |
|---|---|
| `index.html` | Home |
| `menu/index.html` | Menu |
| `about/index.html` | Meet Megan |
| `order/index.html` | Order form + FAQ |
| `404.html` | Not-found page |
| `assets/css/site.css` | All styling. Colors and type sizes are variables at the top. |
| `assets/js/order-mail.js` | Order email logic: item list, lead time, subject/body |
| `assets/js/order.js`, `assets/js/site.js` | Form behavior, mobile nav |
| `assets/img/` | Logo, illustrations, photos, social image, icons |
| `assets/fonts/` | Self-hosted fonts |
| `robots.txt`, `sitemap.xml`, `llms.txt`, `site.webmanifest` | Search-engine and AI-crawler files |
| `CNAME` | Custom domain for GitHub Pages. Do not delete. |
| `tools/` | Optional helper scripts. Not needed to publish. |
| `docs/superpowers/` | The design spec and implementation plan |

## Everyday edits

**Change words.** Open the page, find the text, change it. The header and footer are repeated in every page, so a change there needs to be made in all five files (`index.html`, `menu/`, `about/`, `order/`, `404.html`).

**Add a product.** In `menu/index.html`, follow the `HOW TO ADD A PRODUCT` comment above the card grid. In short: copy the cinnamon-roll card, change its id, name, description, ingredients, and allergens; add the same id and label to `ITEMS` in `assets/js/order-mail.js`; add a matching `<option>` in `order/index.html`; optionally copy the card onto the home page. Remove one placeholder card so the grid stays balanced. Add a `<url>` to `sitemap.xml` only if you add a whole new page.

**Swap in a real photo.** Every product card has a `PHOTO SLOT` comment. Put the photo in `assets/img/` (WebP or JPEG, about 800 px wide, under 150 KB) and replace the `<img>` as the comment shows. Write an alt that describes the photo.

**Expand Megan's bio.** `about/index.html`, look for the `BIO` comment.

**Change the lead time.** `LEAD_DAYS` in `assets/js/order-mail.js`, and the "2 days' notice" text in `order/index.html` (hint, FAQ, and the FAQPage JSON near the top) and in the notes on `menu/index.html`.

**Prices.** There are none by design. If you add them later, put them in the card body and in the `Product` JSON-LD as an `offers` block.

## Things to confirm with Megan

Copy that was written as a safe default and is easy to change:

- Cinnamon roll description ("vanilla-cream glaze", "best eaten warm") on Home and Menu.
- "Please give at least 2 days' notice."
- "Everything is made in a home kitchen that also handles nuts and other allergens" (Menu notes).
- "A reply from Megan herself, usually within a day" and "never from a freezer case" (About).
- "Why Hearth & Honey" wording (About).

## Publishing and the domain

Pushing to `main` publishes. In the repository, Settings → Pages should show: Source "Deploy from a branch", Branch `main` / `/ (root)`, Custom domain `hearthhoneybakery.com`, Enforce HTTPS on.

DNS records at the domain registrar (one-time):

| Type | Host | Value |
|---|---|---|
| A | `@` | `185.199.108.153` |
| A | `@` | `185.199.109.153` |
| A | `@` | `185.199.110.153` |
| A | `@` | `185.199.111.153` |
| CNAME | `www` | `cadejackman-cmyk.github.io` |

After DNS propagates (minutes to a day), GitHub issues the certificate and `www` redirects to the apex. Then submit `https://hearthhoneybakery.com/sitemap.xml` in Google Search Console and Bing Webmaster Tools.

## Tools (optional)

Node 24+ and Chrome installed. From `tools/`: `npm install` once, then:

- `npm run serve` — preview at http://127.0.0.1:8080
- `npm run verify` — unit tests, contrast, HTML validation, links, structured data, axe accessibility, Lighthouse
- `npm run build:images` — regenerate logo/photos/icons from `tools/source/`
- `npm run build:illustrations` — regenerate the SVG roll, swirl, favicon
- `npm run fetch:fonts` — re-download fonts (already committed)

Reports land in `tools/reports/` (ignored by git).
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: README with editing, deployment, and DNS instructions"
```

---

### Task 16: Push and configure GitHub Pages (needs the token)

**Files:** none new.

- [ ] **Step 1: Wait for the token.** Do not proceed without it. The token is used only in memory for these commands and never written to a file, `.git/config`, or memory.

- [ ] **Step 2: Create the repository if it does not exist**

```bash
TOKEN='<paste>'; OWNER=cadejackman-cmyk; REPO=hearth-and-honey
curl -s -o /dev/null -w '%{http_code}\n' -H "Authorization: Bearer $TOKEN" https://api.github.com/repos/$OWNER/$REPO
# 404 → create it:
curl -s -X POST -H "Authorization: Bearer $TOKEN" -H "Accept: application/vnd.github+json" https://api.github.com/user/repos \
  -d '{"name":"hearth-and-honey","description":"Hearth & Honey Bakery website (Draper, Utah)","homepage":"https://hearthhoneybakery.com","private":false,"has_issues":false,"has_wiki":false,"has_projects":false}' | grep -E '"full_name"|"message"'
```
Expected: `"full_name": "cadejackman-cmyk/hearth-and-honey"`. If the API says the token cannot create repositories, ask Cade to create an empty public repo with that name in the GitHub UI and continue.

- [ ] **Step 3: Push**

```bash
cd /c/Users/Cade/hearth-and-honey
git remote add origin https://github.com/cadejackman-cmyk/hearth-and-honey.git 2>/dev/null || true
AUTH=$(printf 'x-access-token:%s' "$TOKEN" | base64 -w0)
git -c http.extraHeader="Authorization: Basic $AUTH" push -u origin main
```
Expected: `main -> main`, branch set up to track.

- [ ] **Step 4: Enable Pages with the custom domain**

```bash
curl -s -X POST -H "Authorization: Bearer $TOKEN" -H "Accept: application/vnd.github+json" \
  https://api.github.com/repos/$OWNER/$REPO/pages -d '{"source":{"branch":"main","path":"/"}}' | grep -E '"html_url"|"message"'
curl -s -X PUT -H "Authorization: Bearer $TOKEN" -H "Accept: application/vnd.github+json" \
  https://api.github.com/repos/$OWNER/$REPO/pages -d '{"cname":"hearthhoneybakery.com"}' -w '%{http_code}\n'
```
Expected: an `html_url` from the first call (or "already exists"), `204` from the second. `https_enforced` can only be switched on after DNS resolves and the certificate is issued; that is a later `PUT` with `{"https_enforced":true}` or one checkbox in Settings → Pages.

- [ ] **Step 5: Confirm the build**

```bash
sleep 60; curl -s -H "Authorization: Bearer $TOKEN" https://api.github.com/repos/$OWNER/$REPO/pages/builds/latest | grep -E '"status"|"error"'
```
Expected: `"status": "built"`. Report to Cade: the repo URL, that Pages is on with the custom domain, and the five DNS records from the README. The site is reachable at the custom domain only after DNS is set; until then GitHub redirects the `github.io` URL to the (unresolved) domain, which is expected.

---

## Self-review notes

- Spec §4 pages → Tasks 9–13. §4.1 sections → Task 9. §4.2 add-product comment → Task 10. §4.3 bio slot → Task 11. §4.4 FAQ + FAQPage → Task 12.
- §5 palette → Tasks 2 and 7. Type → Tasks 5 and 7. Ornament and motion → Tasks 3, 7, 9. Logo vectorization → Task 4.
- §6 form behavior → Tasks 6, 8, 12; no-JS fallback → Task 12 (`.order-fallback`) and CSS `.no-js` rules in Task 7.
- §7 meta/OG/JSON-LD/sitemap/robots/llms/manifest/favicons/CNAME → Tasks 4, 9–13. Lighthouse targets → Task 14.
- §8 accessibility → Tasks 7, 9–12, verified in Task 14.
- §9 repo layout → File Map. §10 deployment → Task 16 and README. §11 verification → Task 14. §12 out of scope → nothing added.
- Names cross-checked: `startServer` (serve.mjs ↔ axe/lighthouse), field ids (order.js ↔ order/index.html), `ITEMS` slugs (order-mail.js ↔ pages), class names (site.css ↔ pages), image filenames (build-images.mjs ↔ pages/JSON-LD/sitemap).
